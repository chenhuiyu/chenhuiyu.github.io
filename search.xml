<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>爬虫-06-多进程的基本原理</title>
    <url>/2020/ck7nf7ddn0004dovse8wg4ubx/</url>
    <content><![CDATA[<h1 id="爬虫-06-多进程的基本原理">爬虫-06-多进程的基本原理</h1>
<p>[toc]</p>
<h2 id="python多线程存在的问题">python多线程存在的问题</h2>
<p>GIL全称为Global Interpreter Lock,即为全局解释所。</p>
<p>在python多线程下，每个线程的执行方式如下：</p>
<ul>
<li>获取GIL</li>
<li>执行对应线程的代码</li>
<li>释放GIL</li>
</ul>
<p>某个线程想要执行，必须先拿到GIL，一个python进程中GIL只有一个，GIL的存在使得Python无论是在单核还是多核的条件下，同一时刻只能运行一个线程，导致python多线程无法发挥多核并行的优势。</p>
<h2 id="python多进程的实现">python多进程的实现</h2>
<p>进程是系统进行资源分配和调度的独立单位，各个进程之间的数据是无法共享的，多个进程无法共享一个全局变量。</p>
<p>Python内置的multiprocessing是用来实现多进程的库。multiprocessing提供了一系列组件，如Process（进程）、Queue（队列）、Semaphore（信号量）、Pipe（管道）、Lock（锁）、Pool（进程池）</p>
<h2 id="process类">Process类</h2>
<p>在multiprocessing中，每个进程都用一个Process类来表示</p>
<p><code>Process([group [, target [, name [, args [, kwargs]]]]])</code></p>
<ul>
<li>target 表示调用对象，可以传入方法的名字</li>
<li>args表示被调用对象位置参数元组，比如target是函数func，他有两个参数m,n，那么args就传入[m,n]即可</li>
<li>kwargs表示调用对象的字典</li>
<li>name是别名，相当于给这个进程取个名字</li>
<li>group分组</li>
</ul>
]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>tutorial</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>爬虫-07-Requests库</title>
    <url>/2020/ck7nf7dcm0000dovsc8b51m13/</url>
    <content><![CDATA[<h1 id="爬虫-07-requests库">爬虫-07-Requests库</h1>
<p>[toc]</p>
<p>爬虫最基础的便是模拟浏览器向服务器发出请求，利用python库可以方便的对网络请求进行模拟，常见的库有urllib、requests等。借助requests库，我们只需关心请求的链接、需要传的参数以及如何设置可选的参数，无需实现底层通信和传输。</p>
<p>当我们在浏览器中输入一个URL并回车，实际上就是让浏览器发起了一个GET类型的HTTP请求，浏览器得到源代码后，把它渲染出来就可以看到网页内容了。</p>
<p>requests提供了一个get方法，我们调用这个方法，传入对应的URL就能得到网页的源代码。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">r=requests.get(<span class="string">'https://static1.scrape.cuiqingcai.com/'</span>)</span><br><span class="line">print(r.text)</span><br></pre></td></tr></table></figure>
<p>输出结果为目标网页的HTML源代码</p>
<h2 id="请求">请求</h2>
<p>下面来了解一下利用requests库构建GET请求的方法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">r=requests.get(<span class="string">'http://httpbin.org/get'</span>)</span><br><span class="line">print(r.text)</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="string">"args"</span>: &#123;&#125;, </span><br><span class="line">  <span class="string">"headers"</span>: &#123;</span><br><span class="line">    <span class="string">"Accept"</span>: <span class="string">"*/*"</span>, </span><br><span class="line">    <span class="string">"Accept-Encoding"</span>: <span class="string">"gzip, deflate"</span>, </span><br><span class="line">    <span class="string">"Host"</span>: <span class="string">"httpbin.org"</span>, </span><br><span class="line">    <span class="string">"User-Agent"</span>: <span class="string">"python-requests/2.18.4"</span>, </span><br><span class="line">    <span class="string">"X-Amzn-Trace-Id"</span>: <span class="string">"Root=1-5e68e6f4-b1c59b00102f4a7278670374"</span></span><br><span class="line">  &#125;, </span><br><span class="line">  <span class="string">"origin"</span>: <span class="string">"158.51.124.182"</span>, </span><br><span class="line">  <span class="string">"url"</span>: <span class="string">"http://httpbin.org/get"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们成功发起了GET请求，返回结果包括了Headers、URL、IP等</p>
<p>我们可以通过params参数传递URL的参数</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">data=&#123;</span><br><span class="line">    <span class="string">'name'</span>:<span class="string">'germey'</span>,</span><br><span class="line">    <span class="string">'age'</span>:<span class="number">25</span></span><br><span class="line">&#125;</span><br><span class="line">r=requests.get(<span class="string">'http://httpbin.org/get'</span>,params=data)</span><br><span class="line">print(r.text)</span><br></pre></td></tr></table></figure>
<p>在这里我们把URL参数通过字典的形式传给get方法的params参数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="string">"args"</span>: &#123;</span><br><span class="line">    <span class="string">"age"</span>: <span class="string">"25"</span>, </span><br><span class="line">    <span class="string">"name"</span>: <span class="string">"germey"</span></span><br><span class="line">  &#125;, </span><br><span class="line">  <span class="string">"headers"</span>: &#123;</span><br><span class="line">    <span class="string">"Accept"</span>: <span class="string">"*/*"</span>, </span><br><span class="line">    <span class="string">"Accept-Encoding"</span>: <span class="string">"gzip, deflate"</span>, </span><br><span class="line">    <span class="string">"Host"</span>: <span class="string">"httpbin.org"</span>, </span><br><span class="line">    <span class="string">"User-Agent"</span>: <span class="string">"python-requests/2.18.4"</span>, </span><br><span class="line">    <span class="string">"X-Amzn-Trace-Id"</span>: <span class="string">"Root=1-5e68e7c7-0daed52e693fe50fdcec097e"</span></span><br><span class="line">  &#125;, </span><br><span class="line">  <span class="string">"origin"</span>: <span class="string">"158.51.124.182"</span>, </span><br><span class="line">  <span class="string">"url"</span>: <span class="string">"http://httpbin.org/get?name=germey&amp;age=25"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>由上述返回信息可知，请求的链接自动被构造成了</p>
<p>&quot;http://httpbin.org/get?name=germey&amp;age=25&quot;</p>
<p>网页返回的类型json格式的str类型，如果想直接解析返回结果，可以直接调用json方法</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">r=requests.get(<span class="string">'http://httpbin.org/get'</span>)</span><br><span class="line">print(type(r.text))</span><br><span class="line">print(r.json())</span><br><span class="line">print(type(r.json()))</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">str</span>'&gt;</span></span><br><span class="line">&#123;'args': &#123;&#125;, 'headers': &#123;'Accept': '*/*', 'Accept-Encoding': 'gzip, deflate', 'Host': 'httpbin.org', 'User-Agent': 'python-requests/2.18.4', 'X-Amzn-Trace-Id': 'Root=1-5e68e918-fc7bf9a0438ed2f0bf1aab90'&#125;, 'origin': '158.51.124.182', 'url': 'http://httpbin.org/get'&#125;</span><br><span class="line">&lt;<span class="class"><span class="keyword">class</span> '<span class="title">dict</span>'&gt;</span></span><br></pre></td></tr></table></figure>
<p>调用json方法，就可以将返回结果是json格式的字符串转换为字典</p>
<h2 id="抓取网页">抓取网页</h2>
<p>上面请求链接返回的是JSON形式的字符串，下面以<code>https://static1.scrape.cuiqingcai.com/</code>为例，提取电影的标题。</p>
<figure>
<img src="https://i.loli.net/2020/03/11/AIUVoCJmP5E6nDG.jpg" alt="Scrape截图.jpg" /><figcaption>Scrape截图.jpg</figcaption>
</figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line">r=requests.get(<span class="string">'https://static1.scrape.cuiqingcai.com/'</span>)</span><br><span class="line">pattern=re.compile(<span class="string">'&lt;h2.*?&gt;(.*?)&lt;/h2&gt;'</span>,re.S)</span><br><span class="line">title=re.findall(pattern,r.text)</span><br><span class="line">print(title)</span><br></pre></td></tr></table></figure>
<p>使用了正则表达式来匹配所有的标题</p>
<h2 id="抓取二进制数据">抓取二进制数据</h2>
<p>上述例子返回的一个HTML文档，如何抓取图片、音频、视频文件呢？这些文件本质都是二进制数据，下面以本博客的站点图标为例：</p>
<figure>
<img src="https://i.loli.net/2020/03/11/FenQqcPKOgxarIt.jpg" alt="图标.jpg" /><figcaption>图标.jpg</figcaption>
</figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line">r=requests.get(<span class="string">'https://chenhuiyu.github.io/cat.ico'</span>)</span><br><span class="line">print(r.text)</span><br></pre></td></tr></table></figure>
<p>这里出现了一些乱码，由于图片是二进制数据，在打印的时候转化为str类型，所以出现了乱码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(r.content)</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">b'\x00\x00\x01\x00\x01\x00  \x00\x00\x01\x00 \x00\xa8\x10\x00\x00\x16\x00\x00\x00(\x00\x00\x00 \x00\x00\x00@\x00\x00\x00\x01\x00</span></span><br></pre></td></tr></table></figure>
<p>content返回了一些二进制代码，b'表示bytes类型的数据，我们把它保存下来</p>
]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>tutorial</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>LaTeX符号速查</title>
    <url>/2020/ck7mctjgn000cf4vs5vcn25a3/</url>
    <content><![CDATA[<p>转载：https://imzlp.me/posts/23224/</p>
<p>[toc]</p>
<h3 id="加减乘除">加减乘除</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">加</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">减</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">乘</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">除</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(+\)</span></td>
<td style="text-align: left;">+</td>
<td style="text-align: left;"><span class="math inline">\(−\)</span></td>
<td style="text-align: left;">-</td>
<td style="text-align: left;"><span class="math inline">\(×\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(÷\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<a id="more"></a>
<h3 id="幂运算">幂运算</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(a^x\)</span></td>
<td style="text-align: left;">a^x</td>
<td style="text-align: left;"><span class="math inline">\(a^{xyz}\)</span></td>
<td style="text-align: left;">a^{xyz}</td>
<td style="text-align: left;"><span class="math inline">\(\sqrt{x}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\sqrt[n]{x}\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="逻辑运算">逻辑运算</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">异或</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">或</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">与</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\oplus\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\vee\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\wedge\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="绝对值">绝对值</h3>
<p>直接插入竖线|即可，使用。</p>
<p><span class="math inline">\(\left| \sum_i \vec{v}_i \left|\Delta t_i\right| \right|\)</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">代码：\left| \sum_i \vec&#123;v&#125;_i \left|\Delta t_i\right| \right|</span><br></pre></td></tr></table></figure>
<h3 id="分式">分式</h3>
<p>参考本文下方的&quot;分式&quot;一节，这里简单说说语法</p>
<p><span class="math inline">\((\frac{a}{x})^2\)</span>语法：<code>(\frac{a}{x} )^2</code></p>
<p>上面的表达式效果是：右边的上标没有对齐，因此可以改进一下：</p>
<p><span class="math inline">\(\left(\frac{a}{x} \right)^2\)</span> 语法：<code>\left(\frac{a}{x} \right)^2</code></p>
<h3 id="其他运算">其他运算</h3>
<p>详见下文操作</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">求和</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">积分</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">积分</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">求积</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\sum\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\int\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(∮\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(∏\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="关系">关系</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(=\)</span></td>
<td style="text-align: left;">=</td>
<td style="text-align: left;"><span class="math inline">\(≠\)</span></td>
<td style="text-align: left;">=</td>
<td style="text-align: left;"><span class="math inline">\(≈\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(≡\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(&lt;\)</span></td>
<td style="text-align: left;">&lt;</td>
<td style="text-align: left;"><span class="math inline">\(&gt;\)</span></td>
<td style="text-align: left;">&gt;</td>
<td style="text-align: left;"><span class="math inline">\(≤\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(≥\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(≪\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(≫\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="集合">集合</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(∈\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(∋\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(⊂\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⊃\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(⊆\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⊇\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="存在">存在</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(∃\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(∀\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="希腊字母">希腊字母</h3>
<p>直接根据拼写输入即可，小写共有22个，大写8个：大写的是小写语法首字母大写。不全部列出了。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;"></th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">小写</td>
<td style="text-align: left;"><span class="math inline">\(α\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\beta\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(γ\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;">大写</td>
<td style="text-align: left;"><span class="math inline">\(Φ\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(Ω\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(Δ\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="箭头">箭头</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(←\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(→\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⇐\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⇒\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⇔\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="省略号">省略号</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(……\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⋯⋯\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="头顶符号">头顶符号</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\hat{x}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\bar{x}\)</span></td>
<td style="text-align: left;">{x}</td>
<td style="text-align: left;"><span class="math inline">\(\vec{x}\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(\dot{x}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\ddot{x}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="标准括号">标准括号</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\((\)</span></td>
<td style="text-align: left;">(</td>
<td style="text-align: left;"><span class="math inline">\()\)</span></td>
<td style="text-align: left;">)</td>
<td style="text-align: left;"><span class="math inline">\([\)</span></td>
<td style="text-align: left;">[</td>
<td style="text-align: left;"><span class="math inline">\(]\)</span></td>
<td style="text-align: left;">]</td>
</tr>
</tbody>
</table>
<h3 id="取整函数">取整函数</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(⌊\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⌋\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(⌈\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⌉\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="空格">空格</h3>
<p>LaTex默认是省略空格，要输入空格就得自己输入命令，mu是一个数学单位。</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">效果</th>
<th style="text-align: left;">说明</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(ab\quad ab\)</span></td>
<td style="text-align: left;">空格宽度是当前字宽(18mu)</td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(ab\, ab\)</span></td>
<td style="text-align: left;">空格宽度是3mu</td>
<td style="text-align: left;">,</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(ab\: ab\)</span></td>
<td style="text-align: left;">空格宽度是4mu</td>
<td style="text-align: left;">:</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(ab\; ab\)</span></td>
<td style="text-align: left;">空格宽度是5mu</td>
<td style="text-align: left;">;</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(a\!ba\!b\)</span></td>
<td style="text-align: left;">空格宽度是-3mu(向左缩)</td>
<td style="text-align: left;">!</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(a\ ba\ b\)</span></td>
<td style="text-align: left;">空格宽度是标准空格键效果</td>
<td style="text-align: left;">在</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(ab\qquad ab\)</span></td>
<td style="text-align: left;">空格宽度是36mu</td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="上标与下标">上标与下标</h3>
<p>使用{..}来告诉MathJax上下表的位置在哪，是谁是大括号</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(2^2\)</span></td>
<td style="text-align: left;">2^2</td>
<td style="text-align: left;"><span class="math inline">\(a_i\)</span></td>
<td style="text-align: left;">a_i</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(2^{23}\)</span></td>
<td style="text-align: left;">2^{23}</td>
<td style="text-align: left;"><span class="math inline">\(n_{i-1}\)</span></td>
<td style="text-align: left;">n_{i-1}</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(a^{i+1}_3\)</span></td>
<td style="text-align: left;">a^{i+1}_3</td>
<td style="text-align: left;"><span class="math inline">\(x^{3^2}\)</span></td>
<td style="text-align: left;">x^{3^2}</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(2^{a_i}\)</span></td>
<td style="text-align: left;">2^{a_i}</td>
<td style="text-align: left;"><span class="math inline">\(2^a_i\)</span></td>
<td style="text-align: left;">2^a_i</td>
</tr>
</tbody>
</table>
<h3 id="上划线下划线">上划线下划线</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">上</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">下</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\overline{a+bi}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\underline{431}\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="分式-1">分式</h3>
<p>有两种尺寸：frac和drac</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">尺寸较小</th>
<th style="text-align: left;">因此适合打印</th>
<th style="text-align: left;">尺寸适中</th>
<th style="text-align: left;">用于显示器展示</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">符号</td>
<td style="text-align: left;">语法</td>
<td style="text-align: left;">符号</td>
<td style="text-align: left;">语法</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(\frac{1}{2}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\dfrac{1}{2}\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\frac{2}{x+2}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\dfrac{2}{x+2}\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(\frac{1+\frac{1}{x}}{3x + 2}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\dfrac{1+\frac{1}{x}}{3x + 2}\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<p>连续（嵌套）使用：<code>\cfrac</code></p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\cfrac{2}{1+\cfrac{2}{1+\cfrac{2}{1+\cfrac{2}{1}}}}\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="根式">根式</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\sqrt(2)\)</span></td>
<td style="text-align: left;">(2)</td>
<td style="text-align: left;"><span class="math inline">\(\sqrt{2}\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(\sqrt{x+y}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\sqrt{x+\frac{1}{2}}\)</span></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\sqrt[3]{3}\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(sqrt[n]{x}\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h3 id="求和-求积-求极限">求和 求积 求极限</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\sum_{i=1}^{\infty}\frac{1}{i}\)</span></td>
<td style="text-align: left;">_{i=1}^{}</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(\prod_{n=1}^5\frac{n}{n-1}\)</span></td>
<td style="text-align: left;">_{n=1}^5</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\lim_{x\to\infty}\frac{1}{x}\)</span></td>
<td style="text-align: left;">_{x}</td>
</tr>
</tbody>
</table>
<h3 id="求微积分-偏导">求微积分 偏导</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\frac{d}{dx}\left(x^2\right) = 2x\)</span></td>
<td style="text-align: left;">(x^2) = 2x</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(\int 2x\,dx = x^2+C\)</span></td>
<td style="text-align: left;">2x,dx = x^2+C</td>
</tr>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\frac{\partial^2U}{\partial x^2} + \frac{\partial^2U}{\partial y^2}\)</span></td>
<td style="text-align: left;"> + </td>
</tr>
</tbody>
</table>
<h3 id="求模">求模</h3>
<p><span class="math inline">\(9\equiv 3 \pmod{6}-\)</span>----- 语法：<code>9\equiv 3 \pmod{6}</code></p>
<h3 id="三角函数">三角函数</h3>
<p>直接输入正常书写的符号即可，不全列举</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">余弦</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">反余弦</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">双曲余弦</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\cos\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\arccos\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(\cosh\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\cos^2 x +\sin^2 x = 1\)</span></td>
<td style="text-align: left;">^2 x +^2 x = 1</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(\cos 90^\circ = 0\)</span></td>
<td style="text-align: left;">90^= 0</td>
</tr>
</tbody>
</table>
<h3 id="矩阵">矩阵</h3>
<p>使用<code>$$\begin{matrix}…\end{matrix}$$</code>，每一行末用<code>\\</code>(在markdown中写LaTeX双反斜杠<code>\\</code>会被转义，所以要使用<code>\\\\</code>)结束，用&amp;分隔矩阵元素。</p>
<p><span class="math display">\[\begin{equation}
H_x=\frac{1}{3}\times{
\left[ \begin{array}{ccc}
1 &amp; 0 &amp; -1\\\\
1 &amp; 0 &amp; -1\\\\
1 &amp; 0 &amp; -1
\end{array} 
\right ]},
H_y=\frac{1}{3}\times{
\left[ \begin{array}{ccc}
1 &amp; 1 &amp; 1\\\\
0&amp; 0 &amp; 0\\\\
1 &amp; 1 &amp; 1
\end{array}
\right ]}
\end{equation}\]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;equation&#125;</span><br><span class="line">H_x&#x3D;\frac&#123;1&#125;&#123;3&#125;\times&#123;</span><br><span class="line">\left[ \begin&#123;array&#125;&#123;ccc&#125;</span><br><span class="line">1 &amp; 0 &amp; -1\\\\</span><br><span class="line">1 &amp; 0 &amp; -1\\\\</span><br><span class="line">1 &amp; 0 &amp; -1</span><br><span class="line">\end&#123;array&#125; </span><br><span class="line">\right ]&#125;,</span><br><span class="line">H_y&#x3D;\frac&#123;1&#125;&#123;3&#125;\times&#123;</span><br><span class="line">\left[ \begin&#123;array&#125;&#123;ccc&#125;</span><br><span class="line">1 &amp; 1 &amp; 1\\\\</span><br><span class="line">0&amp; 0 &amp; 0\\\\</span><br><span class="line">1 &amp; 1 &amp; 1</span><br><span class="line">\end&#123;array&#125;</span><br><span class="line">\right ]&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<h4 id="带省略号的矩阵">带省略号的矩阵</h4>
<p><span class="math display">\[\begin{equation}
S
=\begin{bmatrix}
A  &amp;  B  &amp; \cdots\ &amp;C\\\\
D  &amp;  E  &amp; \cdots\ &amp; F\\\\
 \vdots  &amp; \vdots &amp; \ddots &amp; \vdots \\\\
 G &amp; H  &amp; \cdots\ &amp; I\\\\
\end{bmatrix}
\end{equation}\]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;equation&#125;</span><br><span class="line">S</span><br><span class="line">&#x3D;\begin&#123;bmatrix&#125;</span><br><span class="line">A  &amp;  B  &amp; \cdots\ &amp;C\\\\</span><br><span class="line">D  &amp;  E  &amp; \cdots\ &amp; F\\\\</span><br><span class="line"> \vdots  &amp; \vdots &amp; \ddots &amp; \vdots \\\\</span><br><span class="line"> G &amp; H  &amp; \cdots\ &amp; I\\\\</span><br><span class="line">\end&#123;bmatrix&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">竖直方向</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">水平方向</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">对角线方向</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\vdots\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⋯⋯\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(⋱⋱\)</span></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h4 id="matrix">matrix</h4>
<p><span class="math display">\[\begin{matrix}
A  &amp;  B  &amp; \cdots\ &amp;C\\\\
D  &amp;  E  &amp; \cdots\ &amp; F
\end{matrix}\]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;matrix&#125;</span><br><span class="line">A  &amp;  B  &amp; \cdots\ &amp;C\\\\</span><br><span class="line">D  &amp;  E  &amp; \cdots\ &amp; F</span><br><span class="line">\end&#123;matrix&#125;</span><br></pre></td></tr></table></figure>
<h4 id="pmatrix">pmatrix</h4>
<p><span class="math display">\[\begin{equation}
S
=\begin{pmatrix}
A  &amp;  B  &amp; \cdots\ &amp;C\\\\
D  &amp;  E  &amp; \cdots\ &amp; F
\end{pmatrix}
\end{equation}\]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;equation&#125;</span><br><span class="line">S</span><br><span class="line">&#x3D;\begin&#123;pmatrix&#125;</span><br><span class="line">A  &amp;  B  &amp; \cdots\ &amp;C\\\\</span><br><span class="line">D  &amp;  E  &amp; \cdots\ &amp; F</span><br><span class="line">\end&#123;pmatrix&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<h4 id="bmatrix">bmatrix</h4>
<p><span class="math display">\[\begin{equation}
S
=\begin{bmatrix}
A  &amp;  B  &amp; \cdots\ &amp;C\\\\
D  &amp;  E  &amp; \cdots\ &amp; F
\end{bmatrix}
\end{equation} \]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;equation&#125;</span><br><span class="line">S</span><br><span class="line">&#x3D;\begin&#123;bmatrix&#125;</span><br><span class="line">A  &amp;  B  &amp; \cdots\ &amp;C\\\\</span><br><span class="line">D  &amp;  E  &amp; \cdots\ &amp; F</span><br><span class="line">\end&#123;bmatrix&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<h4 id="bmatrix-1">Bmatrix</h4>
<p>$<span class="math display">\[\begin{equation}
S
=\begin{Bmatrix}
A  &amp;  B  &amp; \cdots\ &amp;C\\\\
D  &amp;  E  &amp; \cdots\ &amp; F
\end{Bmatrix}
\end{equation}\]</span> $</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;equation&#125;</span><br><span class="line">S</span><br><span class="line">&#x3D;\begin&#123;Bmatrix&#125;</span><br><span class="line">A  &amp;  B  &amp; \cdots\ &amp;C\\\\</span><br><span class="line">D  &amp;  E  &amp; \cdots\ &amp; F</span><br><span class="line">\end&#123;Bmatrix&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<h4 id="vmatrix">vmatrix</h4>
<p><span class="math display">\[\begin{equation}
S
=\begin{vmatrix}
A  &amp;  B  &amp; \cdots\ &amp;C\\\\
D  &amp;  E  &amp; \cdots\ &amp; F
\end{vmatrix}
\end{equation} \]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;equation&#125;</span><br><span class="line">S</span><br><span class="line">&#x3D;\begin&#123;vmatrix&#125;</span><br><span class="line">A  &amp;  B  &amp; \cdots\ &amp;C\\\\</span><br><span class="line">D  &amp;  E  &amp; \cdots\ &amp; F</span><br><span class="line">\end&#123;vmatrix&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<h4 id="vmatrix-1">Vmatrix</h4>
<p><span class="math display">\[\begin{equation}
S
=\begin{Vmatrix}
A  &amp;  B  &amp; \cdots\ &amp;C\\\\
D  &amp;  E  &amp; \cdots\ &amp; F
\end{Vmatrix}
\end{equation} \]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\begin&#123;equation&#125;</span><br><span class="line">S</span><br><span class="line">&#x3D;\begin&#123;Vmatrix&#125;</span><br><span class="line">A  &amp;  B  &amp; \cdots\ &amp;C\\\\</span><br><span class="line">D  &amp;  E  &amp; \cdots\ &amp; F</span><br><span class="line">\end&#123;Vmatrix&#125;</span><br><span class="line">\end&#123;equation&#125;</span><br></pre></td></tr></table></figure>
<h4 id="增广矩阵">增广矩阵</h4>
<p><span class="math display">\[\left[
\begin{array}{cc|c}
A &amp; B &amp; C \\\\
D &amp; E &amp; F
\end{array}
\right] \]</span></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">\left[</span><br><span class="line">\begin&#123;array&#125;&#123;cc|c&#125;</span><br><span class="line">A &amp; B &amp; C \\\\</span><br><span class="line">D &amp; E &amp; F</span><br><span class="line">\end&#123;array&#125;</span><br><span class="line">\right]</span><br></pre></td></tr></table></figure>
<h3 id="其他符号">其他符号</h3>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
<th style="text-align: left;">符号</th>
<th style="text-align: left;">语法</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><span class="math inline">\(\infty\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(△\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(∠\)</span></td>
<td style="text-align: left;">$</td>
</tr>
<tr class="even">
<td style="text-align: left;"><span class="math inline">\(✓\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><span class="math inline">\(∇\)</span></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>LaTeX</category>
      </categories>
      <tags>
        <tag>LaTeX</tag>
      </tags>
  </entry>
  <entry>
    <title>DL-01-线性回归</title>
    <url>/2020/ck7mctjfy0000f4vs7uui764n/</url>
    <content><![CDATA[<h1 id="线性回归">线性回归</h1>
<p>[toc]</p>
<p>线性回归输出是一个连续值，因此适用于回归问题。softmax回归则适用于分类问题。线性回归和softmax回归都是单层神经网络，以线性回归为例，介绍大多数深度学习模型的基本要素和表示方法。</p>
<h2 id="线性回归的基本要素">1 线性回归的基本要素</h2>
<p>我们以一个简单的房屋价格预测作为例子来解释线性回归的基本要素。这个应用的目标是预测一栋房子的售出价格（元）。为了简单起见，这里我们假设价格只取决于房屋状况的两个因素，即面积（平方米）和房龄（年）。接下来我们希望探索价格与这两个因素的具体关系。</p>
<h3 id="模型">1.1 模型</h3>
<p>设房屋的面积为<span class="math inline">\(x1\)</span>，房龄为<span class="math inline">\(x2\)</span>，售出价格为<span class="math inline">\(y\)</span>。我们需要建立基于输入<span class="math inline">\(x1\)</span>和<span class="math inline">\(x2\)</span>来计算输出<span class="math inline">\(y\)</span>的表达式，也就是模型（model）。线性回归假设输出与各个输入之间是线性关系：</p>
<p><span class="math display">\[\hat{y}=x_1w_1+x_2w_2+b\]</span></p>
<p>其中<span class="math inline">\(w_1\)</span>和<span class="math inline">\(w_2\)</span>是权重（weight），<span class="math inline">\(b\)</span>是偏差（bias），且均为标量。它们是线性回归模型的参数（parameter）。模型输出<span class="math inline">\(\hat{y}\)</span>是线性回归对真实价格<span class="math inline">\(y\)</span>的预测或估计。我们通常允许它们之间有一定误差。</p>
<a id="more"></a>
<h3 id="模型训练">1.2 模型训练</h3>
<p>接下来我们需要通过数据来寻找特定的模型参数值，使模型在数据上的误差尽可能小。这个过程叫作模型训练（model training）。下面我们介绍模型训练所涉及的3个要素。</p>
<h4 id="训练数据">1.2.1 训练数据</h4>
<p>我们通常收集一系列的真实数据作为训练数据，例如多栋房屋的真实售出价格和它们对应的面积和房龄。我们希望在这个数据上面寻找模型参数来使模型的预测价格与真实价格的误差最小。在机器学习术语里，该数据集被称为训练数据集（training data set）或训练集（training set），一栋房屋被称为一个样本（sample），其真实售出价格叫作标签（label），用来预测标签的两个因素叫作特征（feature）。特征用来表征样本的特点。</p>
<p>假设我们采集的样本数为<span class="math inline">\(n\)</span>，索引为<span class="math inline">\(i\)</span>的样本的特征为<span class="math inline">\(x_1^{(i)}\)</span>和<span class="math inline">\(x_2^{(i)}\)</span>，标签为<span class="math inline">\(y^{(i)}\)</span>。对于索引为<span class="math inline">\(i\)</span>的房屋，线性回归模型的房屋价格预测表达式为</p>
<p><span class="math display">\[\hat{y}^{(i)} = x_1^{(i)} w_1 + x_2^{(i)} w_2 + b.\]</span></p>
<h4 id="损失函数">1.2.2 损失函数</h4>
<p>在模型训练中，我们需要衡量价格预测值与真实值之间的误差。通常我们会选取一个非负数作为误差，且数值越小表示误差越小。一个常用的选择是平方函数。它在评估索引为<span class="math inline">\(i\)</span>的样本误差的表达式为</p>
<p><span class="math display">\[\ell^{(i)}(w_1, w_2, b) = \frac{1}{2} \left(\hat{y}^{(i)} - y^{(i)}\right)^2,\]</span></p>
<p>其中常数<span class="math inline">\(1/2\)</span>使对平方项求导后的常数系数为1，这样在形式上稍微简单一些。显然，误差越小表示预测价格与真实价格越相近，且当二者相等时误差为0。给定训练数据集，这个误差只与模型参数相关，因此我们将它记为以模型参数为参数的函数。在机器学习里，将衡量误差的函数称为损失函数（loss function）。这里使用的平方误差函数也称为平方损失（square loss）。</p>
<p>通常，我们用训练数据集中所有样本误差的平均来衡量模型预测的质量，即</p>
<p><span class="math display">\[\ell(w_1, w_2, b) =\frac{1}{n} \sum_{i=1}^n \ell^{(i)}(w_1, w_2, b) =\frac{1}{n} \sum_{i=1}^n \frac{1}{2}\left(x_1^{(i)} w_1 + x_2^{(i)} w_2 + b - y^{(i)}\right)^2.\]</span></p>
<p>在模型训练中，我们希望找出一组模型参数，记为<span class="math inline">\(w_1^*, w_2^*, b^*\)</span>，来使训练样本平均损失最小：</p>
<p><span class="math display">\[w_1^*, w_2^*, b^* = \operatorname*{argmin}\limits_{w_1, w_2, b}\  \ell(w_1, w_2, b).\]</span></p>
<h4 id="优化算法">1.2.3 优化算法</h4>
<p>当模型和损失函数形式较为简单时，上面的误差最小化问题的解可以直接用公式表达出来。这类解叫作解析解（analytical solution）。本节使用的线性回归和平方误差刚好属于这个范畴。然而，大多数深度学习模型并没有解析解，只能通过优化算法有限次迭代模型参数来尽可能降低损失函数的值。这类解叫作数值解（numerical solution）。</p>
<p>在求数值解的优化算法中，小批量随机梯度下降（mini-batch stochastic gradient descent）在深度学习中被广泛使用。它的算法很简单：先选取一组模型参数的初始值，如随机选取；接下来对参数进行多次迭代，使每次迭代都可能降低损失函数的值。在每次迭代中，先随机均匀采样一个由固定数目训练数据样本所组成的小批量（mini-batch）<span class="math inline">\(\mathcal{B}\)</span>，然后求小批量中数据样本的平均损失有关模型参数的导数（梯度），最后用此结果与预先设定的一个正数的乘积作为模型参数在本次迭代的减小量。</p>
<p>在训练本节讨论的线性回归模型的过程中，模型的每个参数将作如下迭代：</p>
<p><span class="math display">\[
\begin{aligned}
w_1 &amp;\leftarrow w_1 -   \frac{\eta}{|\mathcal{B}|} \sum_{i \in \mathcal{B}} \frac{ \partial \ell^{(i)}(w_1, w_2, b)  }{\partial w_1} = w_1 -   \frac{\eta}{|\mathcal{B}|} \sum_{i \in \mathcal{B}}x_1^{(i)} \left(x_1^{(i)} w_1 + x_2^{(i)} w_2 + b - y^{(i)}\right),\\
w_2 &amp;\leftarrow w_2 -   \frac{\eta}{|\mathcal{B}|} \sum_{i \in \mathcal{B}} \frac{ \partial \ell^{(i)}(w_1, w_2, b)  }{\partial w_2} = w_2 -   \frac{\eta}{|\mathcal{B}|} \sum_{i \in \mathcal{B}}x_2^{(i)} \left(x_1^{(i)} w_1 + x_2^{(i)} w_2 + b - y^{(i)}\right),\\
b &amp;\leftarrow b -   \frac{\eta}{|\mathcal{B}|} \sum_{i \in \mathcal{B}} \frac{ \partial \ell^{(i)}(w_1, w_2, b)  }{\partial b} = b -   \frac{\eta}{|\mathcal{B}|} \sum_{i \in \mathcal{B}}\left(x_1^{(i)} w_1 + x_2^{(i)} w_2 + b - y^{(i)}\right).
\end{aligned}
\]</span> 在上式中，<span class="math inline">\(|\mathcal{B}|\)</span>代表每个小批量中的样本个数（批量大小，batch size），<span class="math inline">\(\eta\)</span>称作学习率（learning rate）并取正数。需要强调的是，这里的批量大小和学习率的值是人为设定的，并不是通过模型训练学出的，因此叫作超参数（hyperparameter）。我们通常所说的“调参”指的正是调节超参数，例如通过反复试错来找到超参数合适的值。在少数情况下，超参数也可以通过模型训练学出。</p>
<h3 id="模型预测">1.3 模型预测</h3>
<p>模型训练完成后，我们将模型参数<span class="math inline">\(w_1, w_2, b\)</span>在优化算法停止时的值分别记作<span class="math inline">\(\hat{w}_1, \hat{w}_2, \hat{b}\)</span>。注意，这里我们得到的并不一定是最小化损失函数的最优解<span class="math inline">\(w_1^*, w_2^*, b^*\)</span>，而是对最优解的一个近似。然后，我们就可以使用学出的线性回归模型<span class="math inline">\(x_1 \hat{w}_1 + x_2 \hat{w}_2 + \hat{b}\)</span>来估算训练数据集以外任意一栋面积（平方米）为<span class="math inline">\(x_1\)</span>、房龄（年）为<span class="math inline">\(x_2\)</span>的房屋的价格了。这里的估算也叫作模型预测、模型推断或模型测试。</p>
<h2 id="线性回归的表示方法">2. 线性回归的表示方法</h2>
<p>线性回归与神经网络的联系，以及线性回归的矢量计算表达式。</p>
<h3 id="神经网络图">2.1 神经网络图</h3>
<p>为了更清晰地展示线性回归作为神经网络的结构，下图使用神经网络图表示本节中介绍的线性回归模型。神经网络图隐去了模型参数权重和偏差。</p>
<figure>
<img src="http://zh.d2l.ai/_images/linreg.svg" alt="线性回归是一个单层神经网络" /><figcaption>线性回归是一个单层神经网络</figcaption>
</figure>
<p>在图1所示的神经网络中，输入分别为<span class="math inline">\(x_1\)</span>和<span class="math inline">\(x_2\)</span>，因此输入层的输入个数为2。输入个数也叫<strong>特征数</strong>或<strong>特征向量维度</strong>。图1中网络的输出为<span class="math inline">\(o\)</span>，输出层的输出个数为1。需要注意的是，我们直接将图1中神经网络的输出<span class="math inline">\(o\)</span>作为线性回归的输出，即<span class="math inline">\(\hat{y} = o\)</span>。由于输入层并不涉及计算，按照惯例，图1所示的神经网络的层数为1。所以，线性回归是一个单层神经网络。输出层中负责计算<span class="math inline">\(o\)</span>的单元又叫神经元。在线性回归中，<span class="math inline">\(o\)</span>的计算依赖于<span class="math inline">\(x_1\)</span>和<span class="math inline">\(x_2\)</span>。也就是说，输出层中的神经元和输入层中各个输入完全连接。因此，这里的输出层又叫全连接层（fully-connected layer）或稠密层（dense layer）。</p>
<h3 id="矢量计算表达式">2.2 矢量计算表达式</h3>
<p>在模型训练或预测时，我们常常会同时处理多个数据样本并用到矢量计算。我们应该尽可能采用矢量计算，以提升计算效率。如果我们对训练数据集里的3个房屋样本（索引分别为1、2和3）逐一预测价格，将得到</p>
<p><span class="math display">\[
\begin{aligned}
\hat{y}^{(1)} &amp;= x_1^{(1)} w_1 + x_2^{(1)} w_2 + b,\\
\hat{y}^{(2)} &amp;= x_1^{(2)} w_1 + x_2^{(2)} w_2 + b,\\
\hat{y}^{(3)} &amp;= x_1^{(3)} w_1 + x_2^{(3)} w_2 + b.
\end{aligned}
\]</span></p>
<p>现在，我们将上面3个等式转化成矢量计算。设</p>
<p><span class="math display">\[
\boldsymbol{\hat{y}} =
\begin{bmatrix}
    \hat{y}^{(1)} \\
    \hat{y}^{(2)} \\
    \hat{y}^{(3)}
\end{bmatrix},\quad
\boldsymbol{X} =
\begin{bmatrix}
    x_1^{(1)} &amp; x_2^{(1)} \\
    x_1^{(2)} &amp; x_2^{(2)} \\
    x_1^{(3)} &amp; x_2^{(3)}
\end{bmatrix},\quad
\boldsymbol{w} =
\begin{bmatrix}
    w_1 \\
    w_2
\end{bmatrix}.
\]</span></p>
<p>对3个房屋样本预测价格的矢量计算表达式为<span class="math inline">\(\boldsymbol{\hat{y}} = \boldsymbol{X} \boldsymbol{w} + b,\)</span> 其中的加法运算使用了广播机制</p>
<p>广义上讲，当数据样本数为<span class="math inline">\(n\)</span>，特征数为<span class="math inline">\(d\)</span>时，线性回归的矢量计算表达式为</p>
<p><span class="math display">\[\boldsymbol{\hat{y}} = \boldsymbol{X} \boldsymbol{w} + b,\]</span></p>
<p>其中模型输出<span class="math inline">\(\boldsymbol{\hat{y}} \in \mathbb{R}^{n \times 1}\)</span>， 批量数据样本特征<span class="math inline">\(\boldsymbol{X} \in \mathbb{R}^{n \times d}\)</span>，权重<span class="math inline">\(\boldsymbol{w} \in \mathbb{R}^{d \times 1}\)</span>， 偏差<span class="math inline">\(b \in \mathbb{R}\)</span>。相应地，批量数据样本标签<span class="math inline">\(\boldsymbol{y} \in \mathbb{R}^{n \times 1}\)</span>。设模型参数<span class="math inline">\(\boldsymbol{\theta} = [w_1, w_2, b]^\top\)</span>，我们可以重写损失函数为</p>
<p><span class="math display">\[\ell(\boldsymbol{\theta})=\frac{1}{2n}(\boldsymbol{\hat{y}}-\boldsymbol{y})^\top(\boldsymbol{\hat{y}}-\boldsymbol{y}).\]</span></p>
<p>小批量随机梯度下降的迭代步骤将相应地改写为</p>
<p><span class="math display">\[\boldsymbol{\theta} \leftarrow \boldsymbol{\theta} -   \frac{\eta}{|\mathcal{B}|} \sum_{i \in \mathcal{B}}   \nabla_{\boldsymbol{\theta}} \ell^{(i)}(\boldsymbol{\theta}),\]</span></p>
<p>其中梯度是损失有关3个为标量的模型参数的偏导数组成的向量： <span class="math display">\[
\nabla_{\boldsymbol{\theta}} \ell^{(i)}(\boldsymbol{\theta})=
\begin{bmatrix}
    \frac{ \partial \ell^{(i)}(w_1, w_2, b)  }{\partial w_1} \\
    \frac{ \partial \ell^{(i)}(w_1, w_2, b)  }{\partial w_2} \\
    \frac{ \partial \ell^{(i)}(w_1, w_2, b)  }{\partial b}
\end{bmatrix}
=
\begin{bmatrix}
    x_1^{(i)} (x_1^{(i)} w_1 + x_2^{(i)} w_2 + b - y^{(i)}) \\
    x_2^{(i)} (x_1^{(i)} w_1 + x_2^{(i)} w_2 + b - y^{(i)}) \\
    x_1^{(i)} w_1 + x_2^{(i)} w_2 + b - y^{(i)}
\end{bmatrix}
=
\begin{bmatrix}
    x_1^{(i)} \\
    x_2^{(i)} \\
    1
\end{bmatrix}
(\hat{y}^{(i)} - y^{(i)}).
\]</span></p>
<h2 id="star小结">:star:小结</h2>
<ul>
<li>和大多数深度学习模型一样，对于线性回归这样一种单层神经网络，它的基本要素包括模型、训练数据、损失函数和优化算法。</li>
<li>既可以用神经网络图表示线性回归，又可以用矢量计算表示该模型。</li>
<li>应该尽可能采用矢量计算，以提升计算效率。</li>
</ul>
<h2 id="computer练习">:computer:练习</h2>
<ul>
<li><p>使用其他包（如NumPy）或其他编程语言（如MATLAB），比较相加两个向量的两种方法的运行时间。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line">a=np.ones(shape=<span class="number">1000</span>)</span><br><span class="line">b=np.ones(shape=<span class="number">1000</span>)</span><br><span class="line"></span><br><span class="line">start = time()</span><br><span class="line">c = np.zeros(shape=<span class="number">1000</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">1000</span>):</span><br><span class="line">    c[i] = a[i] + b[i]</span><br><span class="line">time() - start</span><br><span class="line"><span class="comment">#out:0.0029985904693603516</span></span><br><span class="line"></span><br><span class="line">start = time()</span><br><span class="line">c = np.zeros(shape=<span class="number">1000</span>)</span><br><span class="line">c=a+b</span><br><span class="line">time() - start</span><br><span class="line"><span class="comment">#out:0.0</span></span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>DeepLearning</category>
      </categories>
      <tags>
        <tag>DL</tag>
      </tags>
  </entry>
  <entry>
    <title>MLHW1-预测PM2.5-LinearRegression</title>
    <url>/2020/ck7mctjgf0006f4vs0xf2f1aa/</url>
    <content><![CDATA[<h1 id="homework-1-linear-regression">Homework 1: Linear Regression</h1>
<p>[toc]</p>
<p><em>李宏毅老师课程ML20作业1</em></p>
<h2 id="题目及要求">题目及要求</h2>
<p>本次作业的资料是从行政院环境环保署空气品质监测网所下载的观测资料。在本作业实作 linear regression 预测出 PM2.5 的数值</p>
<p>本次作业使用丰原站的观测记录，分成 train set 跟 test set。train set 是丰原站每个月的前 20 天所有资料。test set 则是从丰原站剩下的资料中取样出来。</p>
<a id="more"></a>
<ul>
<li><p>train.csv: 每个月前 20 天的完整资料。</p></li>
<li><p>test.csv : 从剩下的资料当中取样出连续的 10 小时为一笔，前九小时的所有观测数据当作 feature，第十小时的 PM2.5 当作 answer。一共取出 240 笔不重複的 test data，请根据 feature 预测这 240 笔的 PM2.5。</p>
<p>Data 含有 18 项观测数据 AMB_TEMP, CH4, CO, NHMC, NO, NO2, NOx, O3, PM10, PM2.5, RAINFALL, RH, SO2, THC, WD_HR, WIND_DIREC, WIND_SPEED, WS_HR。</p></li>
</ul>
<figure>
<img src="https://lh4.googleusercontent.com/2T5jn2-J7Wh4JrOMGPonL9QXPeaaCDDIUwrENelNfXuxRRvhnFz9CE2cQHbPQwtiPfyrICDY0Eec4muzI3OinRvS76bd6SQ02yz4kkh1YNwBgHzLmPvqKf2rAld1dIYNDE_ZPSZPsis" alt="img" /><figcaption>img</figcaption>
</figure>
<p>Link: https://www.kaggle.com/c/ml2020spring-hw1</p>
<p>预测 240 笔 testing data 中的 PM2.5 值，并将预测结果上传至 Kaggle</p>
<p>Upload format : csv file第一行必须是 id,value第二行开始，每行分别为 id 值及预测 PM2.5 数值，以逗号隔开。</p>
<ul>
<li>hw1.sh：请手刻实作 linear regression，方法限使用 gradient descent。 禁止使用 numpy.linalg.lstsq</li>
<li>hw1_best.sh：不限定作法，但套件规定仍必须遵照期初公告。</li>
</ul>
<h2 id="实现">实现</h2>
<h3 id="载入数据">载入数据</h3>
<p>没写完 先占坑</p>
]]></content>
      <categories>
        <category>Machine Learning</category>
      </categories>
      <tags>
        <tag>ML</tag>
        <tag>homework</tag>
      </tags>
  </entry>
  <entry>
    <title>基于SRGAN的超分辨率重建</title>
    <url>/2020/ck7mctjgz000mf4vs2hyfd9nf/</url>
    <content><![CDATA[<h1 id="基于srgan的超分辨率重建">基于SRGAN的超分辨率重建</h1>
<p>[toc]</p>
<h2 id="简介">1. 简介</h2>
<h3 id="超分辨率重建问题">1.1 超分辨率重建问题</h3>
<p>​ 超分辨率重建是指将低分辨率图像(LR)恢复成高分辨率图像(HR)的过程，一张被重建的高分辨率图像被称为超分辨率图像或SR图像。超分辨率重建仍旧是计算机视觉领域一个富有挑战性的问题。首先，超分辨率重建问题是一个不适定问题，对于同一个低分辨率图像，存在多个解决方案，而不是一个唯一的解决方案。 为了限制解决方案空间，通常需要可靠的先验信息。此外，超分辨率重建问题的复杂性随着放大倍数的增加而增加。 在较高的放大系数下，丢失场景细节的恢复变得更加复杂。如何用一大较大的放大系数的情况下仍能恢复画质细节成为研究的难点。目前的大部分研究都聚焦于最小化重建均方误差。以此作为优化的结果会带来很高的PSNR(峰值信噪比)，但是通常的结果是缺乏高频细节以及在感知上不与原图匹配。</p>
<p>定量指标</p>
<ul>
<li><p>PSNR = 峰值信噪比 PSNR是信号最大可能功率和影响它的表示精度的破坏性噪声功率的比值</p></li>
<li><p>SSIM = 结构相似性指数 SSIM测量两个相似图像之间的感知差异</p></li>
</ul>
<h3 id="生成对抗网络">1.2 生成对抗网络</h3>
<p>​ 生成对抗网络（GAN）是一种基于博弈论方法的深度神经网络体系结构，其中模型的两个组成部分，即生成器和判别器。生成器尝试捕获真实示例的分布，以生成新的数据示例。判别器通常是二分类器，它尽可能准确地将生成器产生的的数据与真实的样本区分开。 GAN的优化是一个最小最大(minimax)优化问题。该优化终止于一个鞍点，该鞍点对于生成器而言是最小值，而对于鉴别器而言是最大值。也就是说，优化目标是达到Nash均衡。然后，可以认为生成器已经捕获了真实样本的概率分布。 通过这种方式，生成器学习生成更好，更逼真的图像。</p>
<p>​ GAN适用于超分辨率问题：生成器创建SR图像，使鉴别器无法将其识别为真实HR图像或人为的超分辨输出。 以这种方式，产生具有更好的感知质量的HR图像。</p>
<h2 id="损失函数">2. 损失函数</h2>
<p>SRGAN将损失函数由MSE改为感知损失函数。感知损失是内容损失和对抗性损失的加权总和。</p>
<p><span class="math inline">\(l^{SR}=l^{SR}_X+10^{-3}l^{SR}\_{Gen}\)</span></p>
<p>其中<span class="math inline">\(l^{SR}\)</span>叫做感知损失，<span class="math inline">\(l^{SR}_X\)</span>是内容损失，<span class="math inline">\(10^{-3}l^{SR}_{Gen}\)</span>是对抗损失</p>
<h3 id="内容损失">2.1 内容损失</h3>
<p>内容损失有两种类型：</p>
<h4 id="pixel-wise-mse损失">2.1.1 Pixel-wise MSE损失</h4>
<p>​ 逐个像素的MSE损失（Pixel-wise MSE）是真实图像中每个像素与生成图像中一个像素之间的平方误差。Pixel-wise MSE强调的是两幅图像之间每个对应像素的匹配，这与人眼的感知结果有所区别。通过pixel-wise loss训练的图片通常会较为平滑，缺少高频信息。即使输出图片具有较高的PSNR，视觉效果也并没有很突出。</p>
<p><span class="math display">\[l^{SR}_{MSE}=\frac{1}{r^2WH}\sum\limits^{rW}_{x=1}\sum\limits^{rH}_{y=1}(I^{HR}_{x,y}-G_{\theta_{G}}(I^{LR})_{x,y})^2\]</span></p>
<ul>
<li><span class="math inline">\(W,H\)</span>：图像的宽度和高度</li>
<li><span class="math inline">\(r\)</span>：下采样因子</li>
<li><span class="math inline">\(G_{\theta_{G}}\)</span>：生成器，一个前馈CNN网络</li>
<li><span class="math inline">\(\theta_{G}={W_{1:L;b{1:L}}}\)</span>:L层神经网络的参数，包括Weight和bias</li>
<li><span class="math inline">\(G_{\theta_{G}}(I^{LR})\)</span>：由生成器生成的高分辨率图像，</li>
</ul>
<a id="more"></a>
<h4 id="vgg损失">2.1.2 VGG损失</h4>
<p>​ VGG损失是生成的图像和真实图像的特征图之间的欧式距离。VGG19是一种非常流行的深度神经网络，主要用于图像分类。预训练的VGG19网络的中间层用作特征提取器，可用于提取生成的图像和真实图像的特征图。 VGG损失基于这些提取的特征图。 计算为生成的图像和真实图像的特征图之间的欧几里得距离。</p>
<p><span class="math display">\[l^{SR}_{VGG/i,j}=\frac{1}{W_{i,j}H_{i,j}}\sum\limits^{W_{i,j}}_{x=1}\sum\limits^{H_{i,j}}_{y=1}(\phi_{i,j} (I^{HR})_{x,y}-\phi_{i,j}(G_{\theta_{G}}(I^{LR}))_{x,y})^2\]</span></p>
<ul>
<li><span class="math inline">\(\phi_{i,j}\)</span>:VGG19网络中在<span class="math inline">\(j\)</span>层卷积层和激活函数之后，在<span class="math inline">\(i\)</span>层池化层之前的特征图(feature map)</li>
<li><span class="math inline">\(W_{i,j},H_{i,j}\)</span>：特征图的维度参数</li>
</ul>
<h3 id="对抗损失">2.2 对抗损失</h3>
<p>对抗损失是根据判别器提供的概率计算的：</p>
<p><span class="math display">\[l^{SR}_{Gen}=\sum\limits^N_{n-1}-logD_{\theta_D}(G_{\theta_G}(I^{LR}))\]</span></p>
<ul>
<li><span class="math inline">\(D_{\theta_D}(G_{\theta_G}(I^{LR}))\)</span>：是判别器D认为重建图像<span class="math inline">\(G_{\theta_G}(I^{LR})\)</span>是真实图像的可能性</li>
</ul>
<p>训练判别器是要解一个最大化问题：</p>
<p><span class="math display">\[\min\limits_{\theta_G}\max\limits_{\theta_D}\mathbb E_{I^{HR}\sim P_{train}(I^{HR})}[logD_{\theta_D}(I^{HR})]+\mathbb E_{I^{LR}\sim P_G(I^{LR})}[log(1-D_{\theta_D}(G_{\theta_G}(I^{HR}))]\]</span></p>
<ul>
<li>最大化<span class="math inline">\(\theta_D\)</span>，因此<span class="math inline">\(D_{\theta_D}(I^{HR})\)</span>要最大化，这表示让判别器尽可能给真实高分辨率图像高的可能性</li>
<li>最小化<span class="math inline">\(\theta_G\)</span>，因此<span class="math inline">\(D_{\theta_D}(G_{\theta_G}(I^{HR}))\)</span>要最大化，这表示让生成器尽量生成足以使判别器难以分辨的重建图像</li>
</ul>
<h2 id="srgan模型">3. SRGAN模型</h2>
<p>高分辨率图像（HR）被下采样为低分辨率图像（LR）。 GAN生成器用于将其上采样到超分辨率图像（SR）。 我们使用鉴别器来识别HR图像，并反向传播GAN损失以训练鉴别器和生成器。</p>
<figure>
<img src="https://i.loli.net/2020/03/09/SvJ6Cg1cuNpis9y.png" alt="TIM截图20200309030326.png" /><figcaption>TIM截图20200309030326.png</figcaption>
</figure>
<p>这是生成器和判别器网络的体系结构，其中为每个卷积层指示了相应的内核大小(<span class="math inline">\(k\)</span>)，特征图和(<span class="math inline">\(k\)</span>)，步幅(<span class="math inline">\(s\)</span>)。</p>
<p>生成器：输入<span class="math inline">\(\to\)</span>卷积层$<span class="math inline">\(16个残差块\)</span><span class="math inline">\(卷积层\)</span><span class="math inline">\(BN层\)</span><span class="math inline">\(按元素求和\)</span><span class="math inline">\(卷积层\)</span><span class="math inline">\(2层上采样\)</span><span class="math inline">\(卷积层\)</span>$输出。</p>
<p>判别器：VGG<span class="math inline">\(\to\)</span>卷积层<span class="math inline">\(\to\)</span>LeakyReLU$<span class="math inline">\(7层卷积、BN层\)</span><span class="math inline">\(全连接\)</span><span class="math inline">\(LeaklyReLU\)</span><span class="math inline">\(全连接\)</span>$输出</p>
<h3 id="生成器">3.1 生成器</h3>
<h4 id="残差块">3.1.1 残差块</h4>
<p>​ 随着网络层数的增加，网络发生了退化的现象：随着网络层数的增多，训练集loss逐渐下降，然后趋于饱和，当再增加网络深度的话，训练集loss反而会增大。当网络退化时，浅层网络能够达到比深层网络更好的训练效果，这时如果把低层的特征传到高层，那么效果应该至少不比浅层的网络效果差，而ResNet加入了直接映射，保证了 <span class="math inline">\(l+1\)</span>层的网络一定比<span class="math inline">\(l\)</span>层包含更多的图像信息。</p>
<p>​ 残差网络是由一系列残差块组成的。一个残差块可以表示为：<span class="math display">\[x_{l+1}=h(x_l)+\mathcal F(x_l,W_l)\]</span>。残差块由两部分组成：直接映射<span class="math inline">\(h(x_l)\)</span>和残差部分<span class="math inline">\(\mathcal F(x_l,W_l)\)</span>。下图是5种不同的残差块的结构，SRGAN的生成器采用了原始的(a)类残差块。并且激活函数使用了PRelu。</p>
<figure>
<img src="https://i.loli.net/2020/03/09/6tiPNnphTlkzY9C.png" alt="TIM截图20200309042154.png" /><figcaption>TIM截图20200309042154.png</figcaption>
</figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#定义残差块</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">residual_block</span><span class="params">(x)</span>:</span></span><br><span class="line">    filters = [<span class="number">64</span>, <span class="number">64</span>]</span><br><span class="line">    kernel_size = <span class="number">3</span></span><br><span class="line">    strides = <span class="number">1</span></span><br><span class="line">    padding = <span class="string">"same"</span></span><br><span class="line">    momentum = <span class="number">0.8</span></span><br><span class="line">    activation = <span class="string">"relu"</span></span><br><span class="line"></span><br><span class="line">    res = Conv2D(filters=filters[<span class="number">0</span>], kernel_size=kernel_size, strides=strides, padding=padding)(x)</span><br><span class="line">    res = Activation(activation=activation)(res)</span><br><span class="line">    res = BatchNormalization(momentum=momentum)(res)</span><br><span class="line">    res = Conv2D(filters=filters[<span class="number">1</span>], kernel_size=kernel_size, strides=strides, padding=padding)(res)</span><br><span class="line">    res = BatchNormalization(momentum=momentum)(res)</span><br><span class="line"></span><br><span class="line">    res = Add()([res, x])</span><br><span class="line">    <span class="keyword">return</span> res</span><br></pre></td></tr></table></figure>
<h4 id="上采样">3.1.2 上采样：</h4>
<p>​ PixelShuffle(像素重组)的主要功能是将低分辨的特征图，通过卷积和多通道间的重组得到高分辨率的特征图。对特征图做上采样，生成器中使用了2个亚像素卷积。</p>
<figure>
<img src="https://i.loli.net/2020/03/09/iR7rnpTkoBvS1fK.png" alt="1.png" /><figcaption>1.png</figcaption>
</figure>
<p>​ 上图展示了如何进行亚像素卷积，前面就是普通的CNN网络，在模型倒数第二层学习对应的通道数<span class="math inline">\(r^2c\)</span>的卷积，其中<span class="math inline">\(c\)</span>为最终的通道数，如输出是RGB图，则<span class="math inline">\(c\)</span>为3，如输出是灰度图，则<span class="math inline">\(c\)</span>为1；<span class="math inline">\(r\)</span>为需要进行的上采样倍数，为正整数倍，不同的上采样倍数只需要调整这一层卷积的通道数即可。如果想对原图放大3倍，那么需要生成出<span class="math inline">\(3^2=9\)</span>个同样大小的特征图。将九个特征图拼成一个大图，这就是亚像素卷积的操作。</p>
<h4 id="生成器实现">3.1.3 生成器实现</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">build_generator</span><span class="params">()</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 生成器中有16个残差块</span></span><br><span class="line">    residual_blocks = <span class="number">16</span></span><br><span class="line">    momentum = <span class="number">0.8</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 低分辨率图像LR的尺寸</span></span><br><span class="line">    input_shape = (<span class="number">64</span>, <span class="number">64</span>, <span class="number">3</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 生成器的输入层</span></span><br><span class="line">    input_layer = Input(shape=input_shape)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 在残差块之前的卷积层</span></span><br><span class="line">    gen1 = Conv2D(filters=<span class="number">64</span>, kernel_size=<span class="number">9</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>, activation=<span class="string">'relu'</span>)(input_layer)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 添加16个残差块</span></span><br><span class="line">    res = residual_block(gen1)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(residual_blocks - <span class="number">1</span>):</span><br><span class="line">        res = residual_block(res)</span><br><span class="line">    </span><br><span class="line">    <span class="comment">#残差块之后的卷积层和BN层</span></span><br><span class="line">    gen2 = Conv2D(filters=<span class="number">64</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(res)</span><br><span class="line">    gen2 = BatchNormalization(momentum=momentum)(gen2)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 把BN层结果gen2和残差前卷积结果gen1按元素相加</span></span><br><span class="line">    gen3 = Add()([gen2, gen1])</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 上采样增加维数</span></span><br><span class="line">    gen4 = UpSampling2D(size=<span class="number">2</span>)(gen3)</span><br><span class="line">    gen4 = Conv2D(filters=<span class="number">256</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(gen4)</span><br><span class="line">    gen4 = Activation(<span class="string">'relu'</span>)(gen4)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 上采样增加维数</span></span><br><span class="line">    gen5 = UpSampling2D(size=<span class="number">2</span>)(gen4)</span><br><span class="line">    gen5 = Conv2D(filters=<span class="number">256</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(gen5)</span><br><span class="line">    gen5 = Activation(<span class="string">'relu'</span>)(gen5)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 输出卷积层</span></span><br><span class="line">    gen6 = Conv2D(filters=<span class="number">3</span>, kernel_size=<span class="number">9</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(gen5)</span><br><span class="line">    output = Activation(<span class="string">'tanh'</span>)(gen6)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 定义generator模型</span></span><br><span class="line">    model = Model(inputs=[input_layer], outputs=[output], name=<span class="string">'generator'</span>)</span><br><span class="line">    <span class="keyword">return</span> model</span><br></pre></td></tr></table></figure>
<h3 id="判别器">3.2 判别器</h3>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">build_discriminator</span><span class="params">()</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 设置超参数</span></span><br><span class="line">    leakyrelu_alpha = <span class="number">0.2</span></span><br><span class="line">    momentum = <span class="number">0.8</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 高分辨率图像HR的尺寸</span></span><br><span class="line">    input_shape = (<span class="number">256</span>, <span class="number">256</span>, <span class="number">3</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 鉴别器的输入大小</span></span><br><span class="line">    input_layer = Input(shape=input_shape)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 8个卷积层，7个BN层  </span></span><br><span class="line">    dis1 = Conv2D(filters=<span class="number">64</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(input_layer)</span><br><span class="line">    dis1 = LeakyReLU(alpha=leakyrelu_alpha)(dis1)</span><br><span class="line"></span><br><span class="line">    dis2 = Conv2D(filters=<span class="number">64</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">2</span>, padding=<span class="string">'same'</span>)(dis1)</span><br><span class="line">    dis2 = LeakyReLU(alpha=leakyrelu_alpha)(dis2)</span><br><span class="line">    dis2 = BatchNormalization(momentum=momentum)(dis2)</span><br><span class="line"></span><br><span class="line">    dis3 = Conv2D(filters=<span class="number">128</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(dis2)</span><br><span class="line">    dis3 = LeakyReLU(alpha=leakyrelu_alpha)(dis3)</span><br><span class="line">    dis3 = BatchNormalization(momentum=momentum)(dis3)</span><br><span class="line"></span><br><span class="line">    dis4 = Conv2D(filters=<span class="number">128</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">2</span>, padding=<span class="string">'same'</span>)(dis3)</span><br><span class="line">    dis4 = LeakyReLU(alpha=leakyrelu_alpha)(dis4)</span><br><span class="line">    dis4 = BatchNormalization(momentum=<span class="number">0.8</span>)(dis4)</span><br><span class="line"></span><br><span class="line">    dis5 = Conv2D(<span class="number">256</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(dis4)</span><br><span class="line">    dis5 = LeakyReLU(alpha=leakyrelu_alpha)(dis5)</span><br><span class="line">    dis5 = BatchNormalization(momentum=momentum)(dis5)</span><br><span class="line"></span><br><span class="line">    dis6 = Conv2D(filters=<span class="number">256</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">2</span>, padding=<span class="string">'same'</span>)(dis5)</span><br><span class="line">    dis6 = LeakyReLU(alpha=leakyrelu_alpha)(dis6)</span><br><span class="line">    dis6 = BatchNormalization(momentum=momentum)(dis6)</span><br><span class="line"></span><br><span class="line">    dis7 = Conv2D(filters=<span class="number">512</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">1</span>, padding=<span class="string">'same'</span>)(dis6)</span><br><span class="line">    dis7 = LeakyReLU(alpha=leakyrelu_alpha)(dis7)</span><br><span class="line">    dis7 = BatchNormalization(momentum=momentum)(dis7)</span><br><span class="line"></span><br><span class="line">    dis8 = Conv2D(filters=<span class="number">512</span>, kernel_size=<span class="number">3</span>, strides=<span class="number">2</span>, padding=<span class="string">'same'</span>)(dis7)</span><br><span class="line">    dis8 = LeakyReLU(alpha=leakyrelu_alpha)(dis8)</span><br><span class="line">    dis8 = BatchNormalization(momentum=momentum)(dis8)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 全连接层</span></span><br><span class="line">    dis9 = Dense(units=<span class="number">1024</span>)(dis8)</span><br><span class="line">    dis9 = LeakyReLU(alpha=<span class="number">0.2</span>)(dis9)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 用于分类的最后一个全连接层</span></span><br><span class="line">    output = Dense(units=<span class="number">1</span>, activation=<span class="string">'sigmoid'</span>)(dis9)</span><br><span class="line">        </span><br><span class="line">    model = Model(inputs=[input_layer], outputs=[output], name=<span class="string">'discriminator'</span>)</span><br><span class="line">    <span class="keyword">return</span> model</span><br></pre></td></tr></table></figure>
<h3 id="预训练vgg模型">3.3 预训练VGG模型</h3>
<p>预训练的VGG19将用于从真实图像和生成的图像中提取特征</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">build_vgg</span><span class="params">()</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 高分辨率图像HR尺寸</span></span><br><span class="line">    input_shape = (<span class="number">256</span>, <span class="number">256</span>, <span class="number">3</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 加载在'Imagenet'上的VGG19与训练模型</span></span><br><span class="line">    vgg = VGG19(weights=<span class="string">"imagenet"</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 从第9层获取输出</span></span><br><span class="line">    vgg.outputs = [vgg.layers[<span class="number">9</span>].output]</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 设置输入层</span></span><br><span class="line">    input_layer = Input(shape=input_shape)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 提取特征</span></span><br><span class="line">    features = vgg(input_layer)</span><br><span class="line">    </span><br><span class="line">    model = Model(inputs=[input_layer], outputs=[features])</span><br><span class="line">    <span class="keyword">return</span> model</span><br></pre></td></tr></table></figure>
<h3 id="gan模型">3.4 GAN模型</h3>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">build_adversarial_model</span><span class="params">(generator, discriminator, vgg)</span>:</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 高分辨率图像输入</span></span><br><span class="line">    input_high_resolution = Input(shape=high_resolution_shape)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 低分辨率图像输入</span></span><br><span class="line">    input_low_resolution = Input(shape=low_resolution_shape)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 从低分辨率图像生成超分辨率重建的图像</span></span><br><span class="line">    generated_high_resolution_images = generator(input_low_resolution)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 从生成的图像中提取特征图</span></span><br><span class="line">    features = vgg(generated_high_resolution_images)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 让我们使GAN内部的判别器停止训练，不需要在生成器学习时训练判别器。</span></span><br><span class="line">    discriminator.trainable = <span class="literal">False</span></span><br><span class="line">    discriminator.compile(loss=<span class="string">'mse'</span>, optimizer=common_optimizer, metrics=[<span class="string">'accuracy'</span>])</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 判别器判断生成的超分辨率重建图像是真实图像的的概率</span></span><br><span class="line">    probs = discriminator(generated_high_resolution_images)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 创建并编译模型</span></span><br><span class="line">    adversarial_model = Model([input_low_resolution, input_high_resolution], [probs, features])</span><br><span class="line">    adversarial_model.compile(loss=[<span class="string">'binary_crossentropy'</span>, <span class="string">'mse'</span>], loss_weights=[<span class="number">1e-3</span>, <span class="number">1</span>], optimizer=common_optimizer)</span><br><span class="line">    <span class="keyword">return</span> adversarial_model</span><br></pre></td></tr></table></figure>
<h2 id="结果展示">4. 结果展示</h2>
<p><img src="https://i.loli.net/2020/03/09/r9BvaOWL5keHMnF.png" alt="gen_0.png" /> <img src="https://i.loli.net/2020/03/09/MmVWqQvCyjApFxu.png" alt="gen_1.png" /> <img src="https://i.loli.net/2020/03/09/bTulPraSAMgqzXh.png" alt="gen_3.png" /> <img src="https://i.loli.net/2020/03/09/i8MYaxyoEdU4Fwv.png" alt="gen_2.png" /> <img src="https://i.loli.net/2020/03/09/BLJsKe3NYvT5Ord.png" alt="gen_7.png" /> <img src="https://i.loli.net/2020/03/09/4Cp3ZrGu2kRdyxP.png" alt="gen_4.png" /> <img src="https://i.loli.net/2020/03/09/4TEApIr9JWCLS5o.png" alt="gen_5.png" /> <img src="https://i.loli.net/2020/03/09/GxhLMFNOJjAikCs.png" alt="gen_6.png" /> <img src="https://i.loli.net/2020/03/09/2qQBIhn1W3fRCmj.png" alt="gen_8.png" /> <img src="https://i.loli.net/2020/03/09/QsAKqxaOUXPDTyM.png" alt="gen_9.png" /></p>
]]></content>
      <categories>
        <category>GAN</category>
      </categories>
      <tags>
        <tag>GAN</tag>
        <tag>paper</tag>
      </tags>
  </entry>
  <entry>
    <title>生成式对抗网络综述</title>
    <url>/2020/ck7mctjhj0014f4vsf6bdg3sk/</url>
    <content><![CDATA[<h1 id="生成式对抗网络综述">生成式对抗网络综述</h1>
<p>[toc]</p>
<h2 id="简介">1. 简介</h2>
<p>​ GAN包含两个模型：生成器和判别器。这两个模型通常由神经网络来实现，生成器尝试捕获真实示例的分布，以生成新的数据示例。判别器通常是二分类器，它尽可能准确地将生成器产生的的数据与真实的样本区分开。 GAN的优化是一个最小最大(minimax)优化问题。该优化终止于一个鞍点，该鞍点对于生成器而言是最小值，而对于鉴别器而言是最大值。也就是说，优化目标是达到Nash平衡[1]。然后，可以认为生成器已经捕获了真实样本的概率分布。</p>
<p>​ 先前的一些工作采用了使两个神经网络相互竞争的概念。GAN属于生成算法。生成算法和判别算法是两类机器学习算法。如果机器学习算法是基于观测数据的完全概率模型，则该算法是生成性的。生成算法由于其广泛的实际应用而变得越来越流行和重要。</p>
<a id="more"></a>
<hr />
<h2 id="算法">2. 算法</h2>
<h3 id="原始gan">2.1 原始GAN</h3>
<p>​ 当模型都是神经网络时，GANs框架很容易实现。为了从真实数据<span class="math inline">\(x\)</span>的分布中学习到生成器的概率分布<span class="math inline">\(p_g\)</span>，定义输入噪声变量的先验为<span class="math inline">\(p_z\)</span>，其中<span class="math inline">\(z\)</span>是噪音变量。然后，从噪声空间到数据空间的映射被记为<span class="math inline">\(G(z,\theta_g)\)</span>，其中<span class="math inline">\(G\)</span>是神经网络结构表示的可微函数，其参数为<span class="math inline">\(\theta_g\)</span>。除<span class="math inline">\(G\)</span>之外，还存在一个神经网络<span class="math inline">\(D(x，θ_d)\)</span>，其参数为<span class="math inline">\(θ_d\)</span>，并且<span class="math inline">\(D(x)\)</span>的输出是单个标量。<span class="math inline">\(D(x)\)</span>表示<span class="math inline">\(x\)</span>来自真实样本数据而非生成器<span class="math inline">\(G\)</span>所产生的概率。首先对判别器<span class="math inline">\(D\)</span>进行训练，使其可以最大可能地分辨数据是来自真实样本数据还是生成器<span class="math inline">\(G\)</span>产生的假数据。其次训练<span class="math inline">\(G\)</span>，同时最小化<span class="math inline">\(log(1- D(G(z)))\)</span>。</p>
<figure>
<img src="https://i.loli.net/2020/03/08/aiEVHpJmhudCs3r.jpg" alt="图一.png" /><figcaption>图一.png</figcaption>
</figure>
<p><strong>GAN的目标函数</strong></p>
<p><span class="math inline">\(\min_\limits{G} \max_\limits{D}V(D,G)=E_{x\sim p_{data}}[logD(x)]+E_{z\sim p_z(z)}[log(1-D(G(z)))]\)</span></p>
<p>尽管理论上存在独特的解决方案，但GAN的训练仍然很困难，并且由于以下几个原因常常不稳定一个困难是来自这样一个事实，即GAN的最佳权重对应于损失函数的鞍点，而不是最小化点。</p>
<h3 id="gan的代表性变体">2.2 GAN的代表性变体</h3>
<p>​ 条件GAN(CGAN)。原始GAN的生成器G学到了数据的分布，生成出来的图片其实是随机的，也就是说这个G的生成过程处于一种没有指导的状态，cGAN相当于在原始GAN的基础上加上一个条件：condition，以此来指导G的生成过程。在生成模型G和判别模型D中同时加入条件约束y来引导数据的生成过程。条件可以是任何补充的信息，如类标签，其它模态的数据等，这样使得GAN能够更好地被应用于跨模态问题，例如图像自动标注。<img src="https://www.zhihu.com/equation?tex=%5Cmax_D+%5C%7B%5Cmathbb%7BE%7D_%7Bx+%5Csim+P_%7Bdata%7D%7D+%5Clog+D%28x%7Cy%29%2B%5Cmathbb%7BE%7D_%7Bx+%5Csim+P_%7BG%7D%7D+%5Clog+%281-%28D%28x%7Cy%29%29%5C%7D" alt="[公式]" style="zoom: 80%;" /></p>
<hr />
<h2 id="原理">3. 原理</h2>
<h3 id="最大似然估计-mle">3.1 最大似然估计 MLE</h3>
<p>最小化<span class="math inline">\(p_{data}(x)\)</span>和<span class="math inline">\(p_g(x)\)</span>之间的Kullback-Leibler散度等于最大化样本数<span class="math inline">\(m\)</span>增加时的对数似然</p>
<p><span class="math inline">\(\theta^*=\arg\ \min_\limits{\theta}KLD(p_{data}||p_{g})\)</span></p>
<h3 id="模式崩溃">3.2 模式崩溃</h3>
<p>​ 模式崩溃是指，生成器学会了仅从几种数据分布模式中生成样本但是会错过许多其他模式，即使整个训练数据中都存在来自缺失模式的样本。GAN的优化目标是一个极小极大(minmax)问题，即 !<span class="math inline">\(\min_\limits{G} \max_\limits{D}V(G,D)\)</span> ，也就是说，优化生成器的时候，最小化的是 <span class="math inline">\(\max_\limits{D}V(G,D)\)</span>。可是我们是迭代优化的，要保证<span class="math inline">\(V(G,D)\)</span>最大化，就需要迭代非常多次，这就导致训练时间很长。如果我们只迭代一次判别器，然后迭代一次生成器，不断循环迭代。这样原先的极小极大问题，就容易变成极大极小(maxmin)问题，可二者是不一样的，即：<span class="math inline">\(\min_\limits{G} \max_\limits{D}V(G,D) \ne \max_\limits{G} \min_\limits{D}V(G,D)\)</span></p>
<p>​ 如果变化为极小极大问题，那么迭代就是这样的，生成器先生成一些样本，然后判别器给出错误的判别结果并惩罚生成器，于是生成器调整生成的概率分布。可是这样往往导致生成器变“懒”，只生成一些简单的，重复的样本，即缺乏多样性，也叫模式崩溃。</p>
<hr />
<h2 id="应用">4. 应用</h2>
<p>​ GAN是强大的生成模型，可以仅通过随机向量<span class="math inline">\(z\)</span>的生成逼真的样本。我们既不需要知道明确的真实数据分布，也不需要任何数学假设。这些优点使GAN可以广泛应用于许多领域，例如图像处理和计算机视觉，顺序数据。</p>
<h3 id="图像处理和计算机视觉">4.1 图像处理和计算机视觉</h3>
<p>​ GAN最成功的应用是在图像处理和计算机视觉中，例如图像超分辨率，图像合成和处理以及视频处理。</p>
<h4 id="超分辨率">4.1.1 超分辨率</h4>
<p>​ SRGAN用于超分辨率的GAN，是第一个能够推断出逼真的自然图像的放大系数的框架。SRGAN中使用GAN和感知损失生成细节丰富的图像。感知损失重点关注中间特征层的误差，而不是输出结果的逐像素误差。避免了生成的高分辨图像缺乏纹理细节信息问题。为了进一步提高SRGAN的视觉质量，Wang等人对其进行了改进，以得到增强型SRGAN（ESRGAN）。例如，ESRGAN引入了“相对”的思想来让判别器由判断绝对的真假概率变成相对为真或为假的概率。基于CycleGAN，提出了Cycle-in-Cycle GAN用于无监督图像超分辨率。</p>
<h4 id="图像合成与处理">4.1.2 图像合成与处理</h4>
<p>​ <strong>人脸生成</strong>：GAN生成的人脸质量逐年提高，从图7中可以看出，基于原始GAN生成的人脸视觉质量较低，只能用作概念证明。 Radford等人使用更好的神经网络架构：深度卷积神经网络来生成人脸。罗斯等。 解决了GAN训练的不稳定问题，允许使用更大的架构，例如ResNet。Karras等人利用多尺度训练，允许以高保真度生成百万像素的面部图像。</p>
<p>​ 面部生成效果较好的原因是，因为只有一类对象，每个对象都是一张脸，大多数脸数据集通常由直视摄像机的人组成。多数人已经将鼻子和眼睛以及其他地标放置在一致的位置上。</p>
<p>​ <strong>通用对象</strong>：让GAN处理像ImageNet这样的分类数据集要困难一些，ImageNet 具有一千种不同的对象类。但是，最近几年我们看到了快速的进步。这些图像的质量逐年提高。</p>
<h4 id="图像翻译">4.1.3 图像翻译</h4>
<p>​ 图像翻译是指把一幅图像从源域变换到目标域。翻译过程中会保持源域图像内容不变，但是风格或者一些其他属性变成目标域。成对图像翻译典型的例子就是pix2pix，pix2pix使用成对数据训练了一个条件GAN，Loss包括GAN的loss和逐像素差loss。使用特征图上的逐像素差作为感知损失替代图片上的逐像素差，以生成人眼感知上更加接近源域的图像。对于无成对训练数据的图像翻译问题，一个典型的例子是CycleGAN。CycleGAN使用两对GAN，将源域数据通过一个GAN网络转换到目标域之后，再使用另一个GAN网络将目标域数据转换回源域，转换回来的数据和源域数据正好是成对的，构成监督信息。</p>
<h3 id="序列生成">4.2 序列生成</h3>
<p>相比于GAN在图像领域的应用，GAN在文本，语音领域的应用要少很多。主要原因有两个：</p>
<ul>
<li>GAN在优化的时候使用BP算法，对于文本，语音这种离散数据，GAN没法直接跳到目标值，只能根据梯度一步步靠近。</li>
<li>对于序列生成问题，每生成一个单词，我们就需要判断这个序列是否合理，可是GAN里面的判别器是没法做到的。除非我们针对每一个step都设置一个判别器，这显然不合理。</li>
</ul>
<p>为了解决上述问题，强化学习中的策略梯度下降（Policy gredient descent）被引入到GAN中的序列生成问题。</p>
<h4 id="自然语言生成">4.2.1 自然语言生成</h4>
<p>​ LeakGAN解决长文本生成问题。我们允许判别网络将其高级提取的特征泄漏给产生网络，以进一步帮助指导。生成器通过一个附加的Manager模块将此类信息信号纳入所有生成步骤，该Manager模块采用当前生成的单词的提取特征并输出潜在矢量来指导Worker模块进行下一单词的生成。LeakGAN目前仍是文本生成的SOTA方法。</p>
<h4 id="音乐生成">4.2.2 音乐生成</h4>
<p>​ RNN-GAN使用LSTM作为生成器和判别器，直接生成整个音频序列。然而，正如上面提到的，音乐当做包括歌词和音符，对于这种离散数据生成问题直接使用GAN存在很多问题，特别是生成的数据缺乏局部一致性。使用神经网络生成符号音乐的常见方法涉及对自回归模型进行重复采样，直到获得完整的输出序列。尽管这样的方法在产生短音乐序列方面显示出了一定的希望，但这通常没有扩展到最终目标序列明显更长的情况下，例如整首音乐。</p>
]]></content>
      <categories>
        <category>GAN</category>
      </categories>
      <tags>
        <tag>GAN</tag>
        <tag>paper</tag>
      </tags>
  </entry>
  <entry>
    <title>爬虫-05-多线程的基本原理</title>
    <url>/2020/ck7mctjhg0011f4vsc99c6ekw/</url>
    <content><![CDATA[<h1 id="爬虫-05-多线程的基本原理">爬虫-05-多线程的基本原理</h1>
<p>在编写爬虫程序时，为了提高效率，想同时运行多个爬虫任务，这里需要设计多进程和多线程的知识。</p>
<p>[toc]</p>
<h2 id="多线程">多线程</h2>
<p><strong>进程：</strong>进程是一个可以独立运行的程序单位，是线程的集合。比如打开一个浏览器，这就开启了一个浏览器进程。一个进程中是可以同时处理很多事情的，比如一个浏览器中可以打开多个选项卡页面，这就是线程，</p>
<a id="more"></a>
<p><strong>线程</strong>：线程是操作系统进行运算调度的最小单位，是进程中最小的运行单元。线程的并发或并行执行使得整个浏览器可以同时运行这么多任务</p>
<p><strong>多线程</strong>：多线程就是一个进程中同时执行多个线程，浏览器就是典型的多线程执行。</p>
<h2 id="并行和并发">并行和并发</h2>
<p><strong>并发</strong>：concurrency。并发是指同一时刻只能有一条指令执行，但是多个线程的对应指令被快速轮换的执行。每个线程被分配的时间片极短，人类无法感知到多个线程在执行上下文切换的操作，因此宏观上看起来是多个线程在同时执行。而在微观上是这个处理器在连续不断的多个线程之间进行切换和执行。</p>
<p><strong>并行</strong>：parallel。同一时刻有多条指令在多个处理器是同时执行，并行必须要依靠多个处理器。</p>
<p>并行只能在多核系统中存在，并发可以出现在多核和单核系统中。</p>
<h2 id="多线程使用场景">多线程使用场景</h2>
<p>在一个进程中，有些操作是比较耗时需要等待的，比如等待数据库的查询结果的返回，等待网页结果的响应。使用多线程，处理器在某个线程等待时去执行其他线程，整体上提高了执行效率。对于爬虫而言，在爬虫向服务器发起请求后，需要等待服务器的响应返回，这属于IO密集型任务。</p>
<h2 id="python实现多线程">Python实现多线程</h2>
<p>python中实现多线程的模块叫做threading</p>
<h3 id="thread直接创建子线程">Thread直接创建子线程</h3>
<p>使用thread类来创建一个线程，创建时需要指定如果target参数为运行的方法名称。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">target</span><span class="params">(second)</span>:</span></span><br><span class="line">        print(<span class="string">f'<span class="subst">&#123;threading.current_thread().name&#125;</span> is running'</span>)</span><br><span class="line">        print(<span class="string">f'<span class="subst">&#123;threading.current_thread().name&#125;</span> sleep <span class="subst">&#123;second&#125;</span>s'</span>)</span><br><span class="line">        time.sleep(second)</span><br><span class="line">        print(<span class="string">f'<span class="subst">&#123;threading.current_thread().name&#125;</span> is ended'</span>)</span><br><span class="line"></span><br><span class="line">print(<span class="string">f'<span class="subst">&#123;threading.current_thread().name&#125;</span> is running'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span>[<span class="number">1</span>,<span class="number">5</span>]:</span><br><span class="line">        t=threading.Thread(target=target,args=[i])</span><br><span class="line">        t.start()</span><br><span class="line">print(<span class="string">f'<span class="subst">&#123;threading.current_thread().name&#125;</span> is ended'</span>)</span><br></pre></td></tr></table></figure>
<p>运行结果如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">MainThread <span class="keyword">is</span> running</span><br><span class="line">Thread<span class="number">-1</span> <span class="keyword">is</span> running</span><br><span class="line">Thread<span class="number">-1</span> sleep <span class="number">1</span>s</span><br><span class="line">Thread<span class="number">-2</span> <span class="keyword">is</span> running</span><br><span class="line">Thread<span class="number">-2</span> sleep <span class="number">5</span>s</span><br><span class="line">MainThread <span class="keyword">is</span> ended</span><br><span class="line">Thread<span class="number">-1</span> <span class="keyword">is</span> ended</span><br><span class="line">Thread<span class="number">-2</span> <span class="keyword">is</span> ended</span><br></pre></td></tr></table></figure>
<p>要使用 <a href="https://docs.python.org/zh-cn/3/tutorial/inputoutput.html#tut-f-strings" target="_blank" rel="noopener">格式化字符串字面值</a> ，请在字符串的开始引号或三引号之前加上一个 <code>f</code> 或 <code>F</code> 。在此字符串中，你可以在 <code>{</code> 和 <code>}</code> 字符之间写可以引用的变量或字面值的 Python 表达式。</p>
<p>首先声明target方法，接受一个second参数。该方法执行time.sleep操作，second是休眠的秒数。线程的名字使用</p>
<p><code>threading.current_thread().name</code>获取。主线程名字为MainThread，子线程名字为Thread-*</p>
<p>使用threading类新建两个线程，target参数就是刚刚定义的方法，args以列表的形式传递，两次循环i分别是1和5。使用<code>start()</code>方法开始线程的运行</p>
<p>三个线程，主线程MainThread和两个线程Thread-1、Thread-2。主线程首先结束，然后Thread-1、Thread-2接连结束。说明主线程没有等待子线程运行完毕，如果需要主线程等待子线程运行完毕后才退出，可以让每个子线程对象调用join方法，实现如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">MainThread <span class="keyword">is</span> running</span><br><span class="line">Thread<span class="number">-1</span> <span class="keyword">is</span> running</span><br><span class="line">Thread<span class="number">-1</span> sleep <span class="number">1</span>s</span><br><span class="line">Thread<span class="number">-1</span> <span class="keyword">is</span> ended</span><br><span class="line">Thread<span class="number">-2</span> <span class="keyword">is</span> running</span><br><span class="line">Thread<span class="number">-2</span> sleep <span class="number">5</span>s</span><br><span class="line">Thread<span class="number">-2</span> <span class="keyword">is</span> ended</span><br><span class="line">MainThread <span class="keyword">is</span> ended</span><br></pre></td></tr></table></figure>
<p>这样主线程必须等待子线程结束后，主线程才能继续运行并结束。</p>
<p>join ()方法：主线程A中，创建了子线程B，并且在主线程A中调用了B.join()，那么，主线程A会在调用的地方等待，直到子线程B完成操作后，才可以接着往下执行，那么在调用这个线程时可以使用被调用线程的join方法。</p>
<p>原型：join([timeout])</p>
<p>里面的参数时可选的，代表线程运行的最大时间，即如果超过这个时间，不管这个此线程有没有执行完毕都会被回收，然后主线程或函数都会接着执行的。</p>
<h3 id="继承thread类创建子线程">继承Thread类创建子线程</h3>
<p>可以通过继承Thread类的方式创建线程，该线程需要执行的方法写在类的run方法里面。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> threading</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span><span class="params">(threading.Thread)</span>:</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self,second)</span>:</span></span><br><span class="line">                threading.Thread.__init__(self)</span><br><span class="line">                self.second=second</span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">                print(<span class="string">f' <span class="subst">&#123;threading.current_thread().name&#125;</span> is running'</span>)</span><br><span class="line">                time.sleep(self.second)</span><br><span class="line">                print(<span class="string">f' <span class="subst">&#123;threading.current_thread().name&#125;</span> is ended'</span>)</span><br><span class="line"></span><br><span class="line">print(<span class="string">f' <span class="subst">&#123;threading.current_thread().name&#125;</span> is running'</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> [<span class="number">1</span>,<span class="number">5</span>]:</span><br><span class="line">        t=MyThread(i)</span><br><span class="line">        t.start()</span><br><span class="line">        t.join()</span><br><span class="line">print(<span class="string">f' <span class="subst">&#123;threading.current_thread().name&#125;</span> is ended'</span>)</span><br></pre></td></tr></table></figure>
<h3 id="守护线程">守护线程</h3>
<p>如果一个线程被设置为守护线程，则表示如果主线程结束了，该守护线程还没有运行完，守护线程将被强制结束。在Python中使用setDaemon方法将某个线程设置为守护线程</p>
<p>程序运行中，执行一个主线程，如果主线程又创建一个子线程，主线程和子线程就分兵两路，分别运行，那么当主线程完成想退出时，会检验子线程是否完成。如果子线程未完成，则主线程会等待子线程完成后再退出。但是有时候我们需要的是，只要主线程完成了，不管子线程是否完成，都要和主线程一起退出，这时就可以用setDaemon方法了。</p>
<h3 id="互斥锁">互斥锁</h3>
<p>在一个进程中的多个线程是共享资源的，比如在一个进程中，有一个全局变量count用来计数，我们声明多个线程，每个线程都给count加1，代码如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">count = <span class="number">0</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span><span class="params">(threading.Thread)</span>:</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">                threading.Thread.__init__(self)</span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">                <span class="keyword">global</span> count</span><br><span class="line">                temp=count+<span class="number">1</span></span><br><span class="line">                time.sleep(<span class="number">0.001</span>)</span><br><span class="line">                count=temp</span><br><span class="line">                </span><br><span class="line">threads=[]</span><br><span class="line"><span class="keyword">for</span> _ <span class="keyword">in</span> range(<span class="number">1000</span>):</span><br><span class="line">        thread=MyThread()</span><br><span class="line">        thread.start()</span><br><span class="line">        threads.append(thread)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> thread <span class="keyword">in</span> threads:</span><br><span class="line">        thread.join()</span><br><span class="line">print(<span class="string">f'final count:<span class="subst">&#123;count&#125;</span>'</span>)</span><br></pre></td></tr></table></figure>
<p>输出：<code>final count:122</code></p>
<p>我们声明了1000个线程，每个线程都是取到当前全局变量<code>count</code>，然后休眠一短时间，在对<code>count</code>赋予新的值。因为<code>count</code>的值是共享的，每个线程都可以在执行<code>temp=count</code>这行代码时拿到当前<code>count</code>的值，这些线程中的一些线程可能是并发或者并行执行的，这就导致不同的线程拿到的可能是同一个<code>count</code>值，导致最后一些线程的<code>count</code>加1操作没有生效。</p>
<p>所以，如果多个线程同时对某个数据进行读取或修改，我们需要对多个线程进行同步，对数据进行加锁保护，也就是线程锁threading.Lock</p>
<p>加锁保护是当某个线程在对数据进行修改之前，先加锁，这样当其他线程就无法对该数据进行修改，只能等待锁被释放。这样保证了在同一时间只有一个线程同时在操作一个数据。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">count = <span class="number">0</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyThread</span><span class="params">(threading.Thread)</span>:</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">                threading.Thread.__init__(self)</span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">run</span><span class="params">(self)</span>:</span></span><br><span class="line">                <span class="keyword">global</span> count</span><br><span class="line">                lock.acquire()</span><br><span class="line">                temp=count+<span class="number">1</span></span><br><span class="line">                time.sleep(<span class="number">0.001</span>)</span><br><span class="line">                count=temp</span><br><span class="line">                lock.release()</span><br><span class="line">                </span><br><span class="line">lock=threading.Lock()               </span><br><span class="line">threads=[]</span><br><span class="line"><span class="keyword">for</span> _ <span class="keyword">in</span> range(<span class="number">1000</span>):</span><br><span class="line">        thread=MyThread()</span><br><span class="line">        thread.start()</span><br><span class="line">        threads.append(thread)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> thread <span class="keyword">in</span> threads:</span><br><span class="line">        thread.join()</span><br><span class="line">print(<span class="string">f'final count:<span class="subst">&#123;count&#125;</span>'</span>)</span><br></pre></td></tr></table></figure>
<p>输出：<code>final count:1000</code></p>
<p>声明一个lock对象，它是threading.Lock的一个实例，然后在run方法里，在获取count之前先加锁，修改完count之后释放锁。</p>
]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>tutorial</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>NLP03-形式语言与自动机.md</title>
    <url>/2020/ck7mctjgh0007f4vs3ai0fnp0/</url>
    <content><![CDATA[<h1 id="第三章-形式语言与自动机">第三章 形式语言与自动机</h1>
<figure>
<img src="https://i.loli.net/2020/03/08/oat5uhJSM3Y8qPj.png" alt="第三周 形式语言与自动机-01.png" /><figcaption>第三周 形式语言与自动机-01.png</figcaption>
</figure>
<a id="more"></a>
<p><img src="https://i.loli.net/2020/03/08/baVdnAPqlNzsKRX.png" alt="第三周 形式语言与自动机-02.png" /> <img src="https://i.loli.net/2020/03/08/12mkZgfSMqCYL7y.png" alt="第三周 形式语言与自动机-03.png" /> <img src="https://i.loli.net/2020/03/08/BakZgbqX8zYCHT5.png" alt="第三周 形式语言与自动机-04.png" /> <img src="https://i.loli.net/2020/03/08/5KBctrywhuIfsEl.png" alt="第三周 形式语言与自动机-05.png" /> <img src="https://i.loli.net/2020/03/08/kIhbKnifyFlGrMm.png" alt="第三周 形式语言与自动机-06.png" /> <img src="https://i.loli.net/2020/03/08/tnYLH7oAaJy8d3k.png" alt="第三周 形式语言与自动机-07.png" /> <img src="https://i.loli.net/2020/03/08/Os3IgVSH6evFiGx.png" alt="第三周 形式语言与自动机-08.png" /> <img src="https://i.loli.net/2020/03/08/vdqRhntOMeks6zS.png" alt="第三周 形式语言与自动机-09.png" /> <img src="https://i.loli.net/2020/03/08/hWzGJwV6vdqSRTO.png" alt="第三周 形式语言与自动机-10.png" /> <img src="https://i.loli.net/2020/03/08/nSICGdHmouB8fbs.png" alt="第三周 形式语言与自动机-11.png" /> <img src="https://i.loli.net/2020/03/08/QU7p2ycfaLt5G1R.png" alt="第三周 形式语言与自动机-12.png" /></p>
<p><img src="https://i.loli.net/2020/03/08/rdBl8On14icFTWq.png" alt="第三周 形式语言与自动机-13.png" /> <img src="https://i.loli.net/2020/03/08/i2uGFbxYRQoW4XC.png" alt="第三周 形式语言与自动机-14.png" /> <img src="https://i.loli.net/2020/03/08/MY6Z34tfNC5hrp9.png" alt="第三周 形式语言与自动机-15.png" /> <img src="https://i.loli.net/2020/03/08/eW3nZBUQ9qt8Ju7.png" alt="第三周 形式语言与自动机-16.png" /> <img src="https://i.loli.net/2020/03/08/4tdGvfUDhuL61ZJ.png" alt="第三周 形式语言与自动机-17.png" /> <img src="https://i.loli.net/2020/03/08/OEhMGf3bN4RC5BH.png" alt="第三周 形式语言与自动机-18.png" /> <img src="https://i.loli.net/2020/03/08/zIZEisjBKLdp9Na.png" alt="第三周 形式语言与自动机-19.png" /></p>
]]></content>
      <categories>
        <category>NLP</category>
      </categories>
      <tags>
        <tag>NLP</tag>
        <tag>note</tag>
      </tags>
  </entry>
  <entry>
    <title>DLHLP——1.课程简介</title>
    <url>/2020/ck7mctjg70002f4vsazor839o/</url>
    <content><![CDATA[<h1 id="dlhlp1.课程简介">DLHLP——1.课程简介</h1>
<hr />
<p>硬train一发是一种信念，是一种浪漫，是人类最原始的冲动，是人类亘古以来的目标。——李宏毅</p>
<hr />
<p>台大李宏毅老师2020年开的深度学习与人类语言处理课程，文档记录课程笔记。</p>
<p>课程YouTube链接：https://www.youtube.com/playlist?list=PLJV_el3uVTsO07RpBYFsXg-bN5Lu0nhdG</p>
<p>课程网站Home：http://speech.ee.ntu.edu.tw/~tlkagk/courses_DLHLP20.html</p>
<a id="more"></a>
<p>[toc]</p>
<h2 id="本课程和其他nlp课程的不同之处">1 本课程和其他NLP课程的不同之处</h2>
<p>常见NLP课程内容——文字：语音=9：1</p>
<p>本课程内容——文字：语音=5：5</p>
<p>1.1 为什么语音非常重要？</p>
<p>语音处理不仅仅包含语音辨识</p>
<p>有些语言是没有文字的，语言处理非常重要</p>
<p>1.2 人类语音非常复杂</p>
<ul>
<li>语音：</li>
</ul>
<p><em>常用多少K赫兹便是多少信息量</em></p>
<p>一秒钟的声音讯号需要一万六千个real number来表示</p>
<p>即使是同一个人说的同一句话，声音讯号也不会一样</p>
<ul>
<li>文字：</li>
</ul>
<p>句子可以含有巨多的词汇，非常复杂</p>
<h2 id="六种语言处理模型">2 六种语言处理模型：</h2>
<figure>
<img src="https://i.loli.net/2020/03/08/iot8lyqIZvFQ5Tb.png" alt="TIM截图20200308003659.png" /><figcaption>TIM截图20200308003659.png</figcaption>
</figure>
<h3 id="输入语音输出文字">2.1 输入语音，输出文字</h3>
<p>语音识别（speech recognition）</p>
<p>ASR：END-to-END——80MB</p>
<p>​ 不是一般的seq2seq的model</p>
<p>传统语音辨识：有很多模组，整个模型会非常大——2GB</p>
<h3 id="输入文字输出语音">2.2 输入文字，输出语音</h3>
<p>语音合成——TTS (text-to-speech）</p>
<h3 id="输入语音输出语音">2.3 输入语音，输出语音</h3>
<p>语音分离（Speech Separation）——鸡尾酒会效应</p>
<p>Voice Conversion——使用one-shot learning</p>
<h3 id="输入语音输出语音的类别">2.4 输入语音，输出语音的类别</h3>
<p>Speaker Recognition</p>
<p>Keyword Spotting：模型要够小（低功耗）</p>
<h3 id="输入文字输出文字">2.5 输入文字，输出文字</h3>
<p>机器翻译</p>
<p>自动摘要</p>
<p>聊天机器人</p>
<p>问答系统</p>
<h3 id="输入文字输出文字的类别">2.6 输入文字，输出文字的类别</h3>
<h2 id="其他课程内容">3. 其他课程内容</h2>
<h3 id="meta-learning">3.1 Meta Learning</h3>
<figure>
<img src="https://i.loli.net/2020/03/08/7f9D8LXeOh5vTRz.png" alt="TIM截图20200308004504.png" /><figcaption>TIM截图20200308004504.png</figcaption>
</figure>
<h3 id="adversarial-attack">3.2 Adversarial Attack</h3>
<h3 id="可解释ai">3.3 可解释AI</h3>
<h2 id="结语-有关硬train一发">结语 有关<strong>硬train一发</strong></h2>
<p>Model=Deep Network</p>
<p>遇到问题用deep learning<strong>硬train一发</strong>就对了</p>
<p>什么是<strong>硬train一发</strong>：</p>
<p>硬train一发是一种信念，是一种浪漫，是人类最原始的冲动，是人类亘古以来的目标。——李宏毅</p>
]]></content>
      <categories>
        <category>DLHLP</category>
      </categories>
      <tags>
        <tag>DLHLP</tag>
        <tag>语音处理</tag>
      </tags>
  </entry>
  <entry>
    <title>NLP01-绪论</title>
    <url>/2020/ck7mctjgk0008f4vsg7ib0l07/</url>
    <content><![CDATA[<h1 id="自然语言处理-第一章绪论">自然语言处理 第一章——绪论</h1>
<figure>
<img src="https://i.loli.net/2020/03/07/mQyYtiSbqzxnJwR.jpg" alt="0001.jpg" /><figcaption>0001.jpg</figcaption>
</figure>
<a id="more"></a>
<p><img src="https://i.loli.net/2020/03/07/9K7zaxhstriWBL6.jpg" alt="0002.jpg" /> <img src="https://i.loli.net/2020/03/07/NOG8suYBlViWD1c.jpg" alt="0003.jpg" /> <img src="https://i.loli.net/2020/03/07/SbrkLm5t6y7IhiD.jpg" alt="0004.jpg" /> <img src="https://i.loli.net/2020/03/07/ufKkVDYhE3Qs7lo.jpg" alt="0005.jpg" /> <img src="https://i.loli.net/2020/03/07/NfbPYnIW3GtC82V.jpg" alt="0006.jpg" /> <img src="https://i.loli.net/2020/03/07/BVHQeox9JgnvDjY.jpg" alt="0007.jpg" /> <img src="https://i.loli.net/2020/03/07/mdbHoBTh2U74vRy.jpg" alt="0008.jpg" /></p>
]]></content>
      <categories>
        <category>NLP</category>
      </categories>
      <tags>
        <tag>NLP</tag>
        <tag>note</tag>
      </tags>
  </entry>
  <entry>
    <title>爬虫-04-Session和Cookies</title>
    <url>/2020/ck7mctjhd000yf4vs9n6sdspd/</url>
    <content><![CDATA[<h1 id="爬虫-04-session和cookies">爬虫-04-Session和Cookies</h1>
<p>[toc]</p>
<p>崔庆才《52轻松搞定网络爬虫》第4讲，版权归拉勾网所有。</p>
<h2 id="静态网页和动态网页">4.1 静态网页和动态网页</h2>
<p>​ 最基本的HTML代码，我们将其保存为一个.html文件，然后把它放在某台具有固定公网IP的主机上，主机上装上Apache或Nginx等服务器，这样这台主机就可以作为服务器了，其他人可以通过访问服务器看到这个页面，这样就搭建了一个最简单的网站。</p>
<p>​ 这种HTML代码编写的网页叫静态网页，其文字、图片均是通过HTML代码指定。静态网页加载速度快，编写简单，但是可维护性差，不能根据URL灵活多变的展示内容。</p>
<p>​ 动态网页可以动态解析URL中的参数的变化，关联数据库并动态呈现不同的页面内容。现在大多数网页都是动态网页，它们不再是一个简单的HTML，而是可能有JSP、PHP、python等语言编写的。</p>
<p>​ 动态网页还可以实现用户登录和注册功能，需要通过Session和Cookies共同作用</p>
<a id="more"></a>
<h2 id="无状态http">4.2 无状态HTTP</h2>
<p>​ HTTP的无状态是指HTTP协议对事务处理是没有记忆能力的，也就是说服务器不知道客户端是什么状态。当我们向服务器发送请求后，服务器解析此请求，然后返回对应的响应，服务器负责完成这个过程，而这个过程是完全独立的，服务器不会记录前后状态的变化，也就是缺少状态记录。</p>
<p>​ 这意味着如果后续要处理前面的信息，则必须重传。由此出现了两个用于保持HTTP状态连接的技术，这就是Session、Cookies。Session在服务器端，Cookies在客户端，有了Cookies浏览器在下次访问网页时会自动附带上它，并发送给服务器，服务器端通过识别Cookies鉴定是哪个用户，再判断是否是登陆状态，进而返回对应的响应。Cookies里面保存了登陆的凭证，在爬虫中，一般把Cookies放在请求头里直接请求</p>
<h2 id="原理">4.3 原理</h2>
<p>​ <strong>Session</strong>：会话，Session对象用来存储特定用户Session所需的属性及配置信息。当用户在应用程序的web页之间跳转时，存储在Session对象中的变量不会丢失。</p>
<p>​ <strong>Cookies</strong>：网站为了辨别用户身份、进行Session跟踪而存储在用户本地终端上的数据Session维持：客户端第一次请求服务器时，服务器会返回一个响应头中带有Set-Cookie字段的响应给客户端，用来标记是哪一个用户，客户端浏览器会把Cookies保存起来。当浏览器下一次请求服务器时，浏览器会把此Cookies放到请求头一起提交给服务器，Cookies携带了Session ID，服务器检查该Cookies即可找到对应的Session是什么，再判断Session来以此辨认用户状态。在成功登录某个网站时，服务器会告诉客户端设置哪些Cookies信息，在后续访问页面时客户端会把Cookies发送给服务器，服务器再找到对应的Session加以判断。如果Session中某些设置登陆状态的变量是有效的，则证明用户处于登陆状态</p>
<h2 id="属性结构">4.4 属性结构</h2>
<p>Cookies中有哪些内容：</p>
<p>我们来看看Cookies都有哪些内容。在浏览器开发者工具中打开Application选项卡，然后在左侧会有一个Storage部分，最后一项即为Cookies，其中每个条目称为Cookie，它有如下几个属性</p>
<figure>
<img src="https://i.loli.net/2020/03/07/69PDuW4mKbYTp28.png" alt="4-1.png" /><figcaption>4-1.png</figcaption>
</figure>
<ul>
<li>name：该Cookie的名称，Cookie一旦创建名称不可更改</li>
<li>value：该Cookie的值，如果值为Unicode字符，需要为字符编码，如果值为二进制数据，需要BASE64编码</li>
<li>Domain：可以访问该Cookie的域名</li>
<li>Path：Cookie的使用路径，如果是/path/，则只有路径为/path/的页面可以访问该Cookie</li>
<li>Expires/Max Age：该Cookie的失效时间</li>
<li>Size：Cookie大小</li>
<li>HttpOnly：如果为true，只有在http headers中会带有此Cookie的信息</li>
<li>Secure：是否仅被使用安全协议传</li>
</ul>
]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>tutorial</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>爬虫-03-爬虫基本原理</title>
    <url>/2020/ck7mctjha000vf4vs9urefho1/</url>
    <content><![CDATA[<h1 id="爬虫-03-爬虫基本原理">爬虫-03-爬虫基本原理</h1>
<p>[toc]</p>
<h2 id="爬虫概述">3.1 爬虫概述</h2>
<p>​ 简单地说，爬虫就是获取网页并提取保存信息的自动化程序</p>
<h2 id="获取网页">3.2 获取网页</h2>
<p>​ 获取网页的源代码，向网站的服务器发送一个请求，返回的响应体便是网页源代码</p>
<p>​ 借助urllib、requests库可以实现HTTP请求操作，请求和响应都可以用库提供的数据结构表示，得到相应之后只需解析数据结构中的Body部分</p>
<a id="more"></a>
<h2 id="提取信息">3.3 提取信息</h2>
<p>​ 如何在网页源代码中提取想要的数据，使杂乱的数据变得条理清晰</p>
<ol type="1">
<li>正则表达式</li>
<li>根据节点属性、CSS选择器、XPath来提取网页信息的库，如Beatutiful Soup、pyquery、lxml等</li>
</ol>
<h2 id="保存数据">3.4 保存数据</h2>
<ol type="1">
<li>简单保存为TXT文本或JSON数据</li>
<li>保存到MySQL或者MongoDB等数据库</li>
<li>借助SFTP保存到远程服务</li>
</ol>
<h2 id="抓取什么样的数据">3.5 抓取什么样的数据</h2>
<ol type="1">
<li>最常见：HTML源代码</li>
<li>JSON字符串（API接口常用）</li>
<li>二进制数据——图片、视频、音频</li>
</ol>
<h2 id="javascript渲染页面">3. 6 JavaScript渲染页面</h2>
<p>​ 有时使用urlib和requests抓取网页时，得到的源代码和浏览器中的不同。因为网页使用Ajax、前端模块化工具构建，整个网页可能是由JavaScript渲染出来的，原始HTML是一个空壳</p>
<p>​ 可使用Selenium、Splash的库来模拟JavaScript渲染</p>
]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>tutorial</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>爬虫-02-Web网页基础</title>
    <url>/2020/ck7mctjh7000tf4vs2hj46553/</url>
    <content><![CDATA[<h1 id="爬虫-02-web网页基础">爬虫-02-Web网页基础</h1>
<p>[toc]</p>
<h2 id="网页的基本组成">2.1 网页的基本组成</h2>
<p>网页可以分为三大部分：HTML、CSS 和 JavaScript。</p>
<p>如果把网页比作一个人的话，HTML 相当于骨架，JavaScript 相当于肌肉，CSS 相当于皮肤，三者结合起来才能形成一个完整的网页。下面我们来分别介绍一下这三部分的功能。</p>
<hr />
<h3 id="html">2.1.1 HTML</h3>
<p>HTML 是用来描述网页的一种语言，其全称叫作 Hyper Text Markup Language，即超文本标记语言。</p>
<p>我们浏览的网页包括文字、按钮、图片和视频等各种复杂的元素，其基础架构就是 HTML。不同类型的元素通过不同类型的标签来表示，如图片用 img 标签表示，视频用 video 标签表示，段落用 p 标签表示，它们之间的布局又常通过布局标签 div 嵌套组合而成，各种标签通过不同的排列和嵌套就可以形成网页的框架。</p>
<a id="more"></a>
<hr />
<h3 id="css">2.1.2 CSS</h3>
<p>CSS，全称叫作 Cascading Style Sheets，即<strong>层叠样式表</strong>。</p>
<ul>
<li><p>“层叠” 是指当在 HTML 中引用了数个样式文件，并且样式发生冲突时，浏览器能依据层叠顺序处理。</p></li>
<li><p>“样式” 指网页中文字大小、颜色、元素间距、排列等格式。</p></li>
</ul>
<p>CSS 是目前唯一的网页页面排版样式标准，有了它的帮助，页面才会变得更为美观。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#head_wrapper</span><span class="selector-class">.s-ps-islite</span> <span class="selector-class">.s-p-top</span> &#123;  </span><br><span class="line">   <span class="attribute">position</span>: absolute;  </span><br><span class="line">   <span class="attribute">bottom</span>: <span class="number">40px</span>;  </span><br><span class="line">   <span class="attribute">width</span>: <span class="number">100%</span>;  </span><br><span class="line">   <span class="attribute">height</span>: <span class="number">181px</span>;</span><br></pre></td></tr></table></figure>
<p>大括号前面是一个 CSS 选择器。此选择器的作用是首先选中 id 为 head_wrapper 且 class 为 s-ps-islite 的节点，然后再选中其内部的 class 为 s-p-top 的节点。</p>
<p>大括号内部写的就是一条条样式规则，例如 position 指定了这个元素的布局方式为绝对布局，bottom 指定元素的下边距为 40 像素，width 指定了宽度为 100% 占满父元素，height 则指定了元素的高度。</p>
<p>也就是说，我们将位置、宽度、高度等样式配置统一写成这样的形式，然后用大括号括起来，接着在开头再加上 CSS 选择器，这就代表这个样式对 CSS 选择器选中的元素生效，元素就会根据此样式来展示了。</p>
<p>在网页中，一般会统一定义整个网页的样式规则，并写入 CSS 文件中（其后缀为 css）。在 HTML 中，只需要用 link 标签即可引入写好的 CSS 文件，这样整个页面就会变得美观、优雅。</p>
<hr />
<h3 id="javascript">2.1.3 JavaScript</h3>
<p>JavaScript，简称 JS，是一种脚本语言。HTML 和 CSS 配合使用，提供给用户的只是一种静态信息，缺乏交互性。我们在网页里可能会看到一些交互和动画效果，如下载进度条、提示框、轮播图等，这通常就是 JavaScript 的功劳。它的出现使得用户与信息之间不只是一种浏览与显示的关系，而是实现了一种实时、动态、交互的页面功能。</p>
<p>JavaScript 通常也是以单独的文件形式加载的，后缀为 js，在 HTML 中通过 script 标签即可引入，例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&lt;script src=<span class="string">"jquery-2.1.0.js"</span>&gt;&lt;<span class="regexp">/script&gt;</span></span><br></pre></td></tr></table></figure>
<hr />
<hr />
<h2 id="网页的结构">2.2 网页的结构</h2>
<p>了解了网页的基本组成，我们再用一个例子来感受下 HTML 的基本结构。新建一个文本文件，名称可以自取，后缀为 html，内容如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">title</span>&gt;</span>This is a Demo<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"container"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"wrapper"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">"title"</span>&gt;</span>Hello World<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"text"</span>&gt;</span>Hello, this is a paragraph.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>这就是一个最简单的 HTML 实例。</p>
<p>开头用 DOCTYPE 定义了文档类型，其次最外层是 html 标签，最后还有对应的结束标签来表示闭合，其内部是 head 标签和 body 标签，分别代表网页头和网页体，它们也需要结束标签。</p>
head 标签内定义了一些页面的配置和引用，如：
<meta charset="UTF-8">
<p>，它指定了网页的编码为 UTF-8。title 标签则定义了网页的标题，会显示在网页的选项卡中，不会显示在正文中。body 标签内则是在网页正文中显示的内容。</p>
<p>div 标签定义了网页中的区块，它的 id 是 container，这是一个非常常用的属性，且 id 的内容在网页中是唯一的，我们可以通过它来获取这个区块。然后在此区块内又有一个 div 标签，它的 class 为 wrapper，这也是一个非常常用的属性，经常与 CSS 配合使用来设定样式。</p>
<p>然后此区块内部又有一个 h2 标签，这代表一个二级标题。另外，还有一个 p 标签，这代表一个段落。在这两者中直接写入相应的内容即可在网页中呈现出来，它们也有各自的 class 属性。</p>
<p>这个实例便是网页的一般结构。一个网页的标准形式是 html 标签内嵌套 head 和 body 标签，head 内定义网页的配置和引用，body 内定义网页的正文。</p>
<h2 id="节点树及节点间的关系">2.3 节点树及节点间的关系</h2>
<p>在 HTML 中，所有标签定义的内容都是节点，它们构成了一个 HTML DOM 树。</p>
<p>DOM 是 W3C（万维网联盟）的标准，其英文全称 Document Object Model，即文档对象模型。它定义了访问 HTML 和 XML 文档的标准：</p>
<blockquote>
<p>W3C 文档对象模型（DOM）是中立于平台和语言的接口，它允许程序和脚本动态地访问和更新文档的内容、结构和样式。</p>
</blockquote>
<p>W3C DOM 标准被分为 3 个不同的部分：</p>
<ul>
<li>核心 DOM - 针对任何结构化文档的标准模型</li>
<li>XML DOM - 针对 XML 文档的标准模型</li>
<li>HTML DOM - 针对 HTML 文档的标准模型</li>
</ul>
<p>根据 W3C 的 HTML DOM 标准，HTML 文档中的所有内容都是节点：</p>
<ul>
<li>整个文档是一个文档节点</li>
<li>每个 HTML 元素是元素节点</li>
<li>HTML 元素内的文本是文本节点</li>
<li>每个 HTML 属性是属性节点</li>
<li>注释是注释节点</li>
</ul>
<p>HTML DOM 将 HTML 文档视作树结构，这种结构被称为节点树，如图所示。</p>
<figure>
<img src="https://i.loli.net/2020/03/07/DXf5U7yRJob8Nrh.jpg" alt="2-1.jpg" /><figcaption>2-1.jpg</figcaption>
</figure>
<h2 id="选择器">2.4 选择器</h2>
<p>我们知道网页由一个个节点组成，CSS 选择器会根据不同的节点设置不同的样式规则，那么怎样来定位节点呢？<strong>在 CSS 中，我们使用 CSS 选择器来定位节点</strong>。最常用的 3 种表示，分别是根据 <strong>id、class、标签名</strong>筛选</p>
<h3 id="id选择节点">2.4.1 id选择节点——</h3>
<p>例如，上例中 div 节点的 id 为 container，那么就可以表示为 #container，其中<strong># 开头代表选择 id，其后紧跟 id 的名称</strong>。</p>
<h3 id="class选择节点.">2.4.2 class选择节点——.</h3>
<p>另外，如果我们想选择 class 为 wrapper 的节点，便可以使用 .wrapper，这里以点<strong>“.”开头代表选择 class，其后紧跟 class 的名称</strong>。</p>
<h3 id="标签名选择节点">2.4.3 标签名选择节点</h3>
<p>另外，还有一种选择方式，那就是根据标签名筛选，例如想选择二级标题，直接用 h2 即可。</p>
<h3 id="css嵌套选择">2.4.4 CSS嵌套选择</h3>
<p>另外，CSS 选择器还支持嵌套选择，各个选择器之间加上空格分隔开便可以代表嵌套关系，如 #container .wrapper p 则代表先选择 id 为 container 的节点，然后选中其内部的 class 为 wrapper 的节点，然后再进一步选中其内部的 p 节点。</p>
<p>另外，如果不加空格，则代表并列关系，如 div#container .wrapper p.text 代表先选择 id 为 container 的 div 节点，然后选中其内部的 class 为 wrapper 的节点，再进一步选中其内部的 class 为 text 的 p 节点。这就是 CSS 选择器，其筛选功能还是非常强大的。</p>
<p>另外，CSS 选择器还有一些其他语法规则，具体如表所示。</p>
<table>
<thead>
<tr class="header">
<th>选　择　器</th>
<th>例　　子</th>
<th>例子描述</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>.class</td>
<td>.intro</td>
<td>选择 class=&quot;intro&quot; 的所有节点</td>
</tr>
<tr class="even">
<td>#id</td>
<td>#firstname</td>
<td>选择 id=&quot;firstname&quot; 的所有节点</td>
</tr>
<tr class="odd">
<td>*</td>
<td>*</td>
<td>选择所有节点</td>
</tr>
<tr class="even">
<td>element</td>
<td>p</td>
<td>选择所有 p 节点</td>
</tr>
<tr class="odd">
<td>element,element</td>
<td>div,p</td>
<td>选择所有 div 节点和所有 p 节点</td>
</tr>
<tr class="even">
<td>element element</td>
<td>div p</td>
<td>选择 div 节点内部的所有 p 节点</td>
</tr>
<tr class="odd">
<td>element&gt;element</td>
<td>div&gt;p</td>
<td>选择父节点为 div 节点的所有 p 节点</td>
</tr>
<tr class="even">
<td>element+element</td>
<td>div+p</td>
<td>选择紧接在 div 节点之后的所有 p 节点</td>
</tr>
<tr class="odd">
<td>[attribute]</td>
<td>[target]</td>
<td>选择带有 target 属性的所有节点</td>
</tr>
<tr class="even">
<td>[attribute=value]</td>
<td>[target=blank]</td>
<td>选择 target=&quot;blank&quot; 的所有节点</td>
</tr>
<tr class="odd">
<td>[attribute~=value]</td>
<td>[title~=flower]</td>
<td>选择 title 属性包含单词 flower 的所有节点</td>
</tr>
<tr class="even">
<td>:link</td>
<td>a:link</td>
<td>选择所有未被访问的链接</td>
</tr>
<tr class="odd">
<td>:visited</td>
<td>a:visited</td>
<td>选择所有已被访问的链接</td>
</tr>
<tr class="even">
<td>:active</td>
<td>a:active</td>
<td>选择活动链接</td>
</tr>
<tr class="odd">
<td>:hover</td>
<td>a:hover</td>
<td>选择鼠标指针位于其上的链接</td>
</tr>
<tr class="even">
<td>:focus</td>
<td>input:focus</td>
<td>选择获得焦点的 input 节点</td>
</tr>
<tr class="odd">
<td>:first-letter</td>
<td>p:first-letter</td>
<td>选择每个 p 节点的首字母</td>
</tr>
<tr class="even">
<td>:first-line</td>
<td>p:first-line</td>
<td>选择每个 p 节点的首行</td>
</tr>
<tr class="odd">
<td>:first-child</td>
<td>p:first-child</td>
<td>选择属于父节点的第一个子节点的所有 p 节点</td>
</tr>
<tr class="even">
<td>:before</td>
<td>p:before</td>
<td>在每个 p 节点的内容之前插入内容</td>
</tr>
<tr class="odd">
<td>:after</td>
<td>p:after</td>
<td>在每个 p 节点的内容之后插入内容</td>
</tr>
<tr class="even">
<td>:lang(language)</td>
<td>p:lang</td>
<td>选择带有以 it 开头的 lang 属性值的所有 p 节点</td>
</tr>
<tr class="odd">
<td>element1~element2</td>
<td>p~ul</td>
<td>选择前面有 p 节点的所有 ul 节点</td>
</tr>
<tr class="even">
<td>[attribute^=value]</td>
<td>a[src^=&quot;https&quot;]</td>
<td>选择其 src 属性值以 https 开头的所有 a 节点</td>
</tr>
<tr class="odd">
<td>[attribute$=value] | a[src$=&quot;.pdf&quot;]</td>
<td>选择其 src 属性以.pdf 结尾的所有 a 节点</td>
<td></td>
</tr>
<tr class="even">
<td>[attribute*=value]</td>
<td>a[src*=&quot;abc&quot;]</td>
<td>选择其 src 属性中包含 abc 子串的所有 a 节点</td>
</tr>
<tr class="odd">
<td>:first-of-type</td>
<td>p:first-of-type</td>
<td>选择属于其父节点的首个 p 节点的所有 p 节点</td>
</tr>
<tr class="even">
<td>:last-of-type</td>
<td>p:last-of-type</td>
<td>选择属于其父节点的最后 p 节点的所有 p 节点</td>
</tr>
<tr class="odd">
<td>:only-of-type</td>
<td>p:only-of-type</td>
<td>选择属于其父节点唯一的 p 节点的所有 p 节点</td>
</tr>
<tr class="even">
<td>:only-child</td>
<td>p:only-child</td>
<td>选择属于其父节点的唯一子节点的所有 p 节点</td>
</tr>
<tr class="odd">
<td>:nth-child(n)</td>
<td>p:nth-child</td>
<td>选择属于其父节点的第二个子节点的所有 p 节点</td>
</tr>
<tr class="even">
<td>:nth-last-child(n)</td>
<td>p:nth-last-child</td>
<td>同上，从最后一个子节点开始计数</td>
</tr>
<tr class="odd">
<td>:nth-of-type(n)</td>
<td>p:nth-of-type</td>
<td>选择属于其父节点第二个 p 节点的所有 p 节点</td>
</tr>
<tr class="even">
<td>:nth-last-of-type(n)</td>
<td>p:nth-last-of-type</td>
<td>同上，但是从最后一个子节点开始计数</td>
</tr>
<tr class="odd">
<td>:last-child</td>
<td>p:last-child</td>
<td>选择属于其父节点最后一个子节点的所有 p 节点</td>
</tr>
<tr class="even">
<td>:root</td>
<td>:root</td>
<td>选择文档的根节点</td>
</tr>
<tr class="odd">
<td>:empty</td>
<td>p:empty</td>
<td>选择没有子节点的所有 p 节点（包括文本节点）</td>
</tr>
<tr class="even">
<td>:target</td>
<td>#news:target</td>
<td>选择当前活动的 #news 节点</td>
</tr>
<tr class="odd">
<td>:enabled</td>
<td>input:enabled</td>
<td>选择每个启用的 input 节点</td>
</tr>
<tr class="even">
<td>:disabled</td>
<td>input:disabled</td>
<td>选择每个禁用的 input 节点</td>
</tr>
<tr class="odd">
<td>:checked</td>
<td>input:checked</td>
<td>选择每个被选中的 input 节点</td>
</tr>
<tr class="even">
<td>:not(selector)</td>
<td>:not</td>
<td>选择非 p 节点的所有节点</td>
</tr>
<tr class="odd">
<td>::selection</td>
<td>::selection</td>
<td>选择被用户选取的节点部分</td>
</tr>
</tbody>
</table>
<p>另外，还有一种比较常用的选择器是 XPath，这种选择方式后面会详细介绍。</p>
]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>tutorial</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>爬虫-01-HTTP基本原理</title>
    <url>/2020/ck7mctjh2000of4vscvxo6ozg/</url>
    <content><![CDATA[<h1 id="爬虫-01-http基本原理">爬虫-01-HTTP基本原理</h1>
<p>// 记录崔庆才老师的《52讲轻松搞定网络爬虫》学习笔记。</p>
<p>[toc]</p>
<h2 id="uri-和-url">URI 和 URL</h2>
<p><strong>URI</strong>的全称为 Uniform Resource Identifier，即<strong>统一资源标志符</strong></p>
<p><strong>URL</strong>的全称为 Universal Resource Locator，即<strong>统一资源定位符</strong></p>
<p>URL 是 URI 的子集，URI 还包括一个子类叫作URN，它的全称为 Universal Resource Name，即统一资源名称。URN 只命名资源而不指定如何定位资源（URN不常使用）</p>
<figure>
<img src="https://i.loli.net/2020/03/07/rK1CnWuBycQsYXZ.jpg" alt="1-1.jpg" /><figcaption>1-1.jpg</figcaption>
</figure>
<a id="more"></a>
<h2 id="超文本-hypertext">超文本 Hypertext</h2>
<p>超文本，其英文名称叫作 Hypertext，我们在浏览器里看到的网页就是超文本解析而成的，其网页源代码是一系列 HTML 代码，里面包含了一系列标签，比如 img 显示图片，p 指定显示段落等。浏览器解析这些标签后，便形成了我们平常看到的网页，而网页的源代码 HTML 就可以称作超文本。</p>
<h2 id="http-和-https">HTTP 和 HTTPS</h2>
<p>URL 的开头会有 http 或 https，这个就是访问资源需要的协议类型，有时我们还会看到 ftp、sftp、smb 开头的 URL，那么这里的 ftp、sftp、smb 都是指的协议类型。在爬虫中，我们抓取的页面通常就是 http 或 https 协议的，我们在这里首先来了解一下这两个协议的含义。</p>
<p><strong>HTTP</strong>的全称是 Hyper Text Transfer Protocol，中文名叫作<strong>超文本传输协议</strong>，HTTP 协议是用于从网络传输超文本数据到本地浏览器的传送协议，它能保证高效而准确地传送超文本文档。HTTP 由万维网协会（World Wide Web Consortium）和 Internet 工作小组 IETF（Internet Engineering Task Force）共同合作制定的规范，目前广泛使用的是 HTTP 1.1 版本。</p>
<p><strong>HTTPS</strong>的全称是 Hyper Text Transfer Protocol over Secure Socket Layer，是以安全为目标的 HTTP 通道，简单讲是 <strong>HTTP 的安全版</strong>，即<strong>HTTP 下加入 SSL 层</strong>，简称为 HTTPS。</p>
<p>HTTPS 的安全基础是 SSL，因此通过它传输的内容都是经过 SSL 加密的，它的主要作用可以分为两种：</p>
<ul>
<li>建立一个信息安全通道，来保证数据传输的安全。</li>
<li>确认网站的真实性，凡是使用了 HTTPS 的网站，都可以通过点击浏览器地址栏的锁头标志来查看网站认证之后的真实信息，也可以通过 CA 机构颁发的安全签章来查询。</li>
</ul>
<h2 id="http-请求过程">HTTP 请求过程</h2>
<p>我们在浏览器中输入一个 URL，回车之后便可以在浏览器中观察到页面内容。实际上，这个过程是浏览器向网站所在的服务器发送了一个请求，网站服务器接收到这个请求后进行处理和解析，然后返回对应的响应，接着传回给浏览器。响应里包含了页面的源代码等内容，浏览器再对其进行解析，便将网页呈现了出来，传输模型如图所示。</p>
<figure>
<img src="https://i.loli.net/2020/03/07/FK5qREk23dDfQ1b.jpg" alt="1-2.jpg" /><figcaption>1-2.jpg</figcaption>
</figure>
<p>用 Chrome 浏览器的开发者模式下的 Network 监听组件来做下演示，它可以显示访问当前请求网页时发生的所有网络请求和响应。</p>
<p>我们先观察第一个网络请求，即 www.baidu.com，其中各列的含义如下。</p>
<figure>
<img src="https://i.loli.net/2020/03/07/QPmZUWghGID42yn.png" alt="1-3.png" /><figcaption>1-3.png</figcaption>
</figure>
<ul>
<li>第一列 Name：请求的名称，一般会将 URL 的最后一部分内容当作名称。</li>
<li>第二列 Status：响应的状态码，这里显示为 200，代表响应是正常的。通过状态码，我们可以判断发送了请求之后是否得到了正常的响应。</li>
<li>第三列 Type：请求的文档类型。这里为 document，代表我们这次请求的是一个 HTML 文档，内容就是一些 HTML 代码。</li>
<li>第四列 Initiator：请求源。用来标记请求是由哪个对象或进程发起的。</li>
<li>第五列 Size：从服务器下载的文件和请求的资源大小。如果是从缓存中取得的资源，则该列会显示 from cache。</li>
<li>第六列 Time：发起请求到获取响应所用的总时间。</li>
<li>第七列 Waterfall：网络请求的可视化瀑布流。</li>
</ul>
<p>我们点击这个条目即可看到其更详细的信息，如图所示。</p>
<figure>
<img src="https://i.loli.net/2020/03/07/U1dL3ADn4cEFuWY.jpg" alt="1-4.jpg" /><figcaption>1-4.jpg</figcaption>
</figure>
<p><strong>General</strong>部分：</p>
<ul>
<li><p>Request URL 为请求的 URL</p></li>
<li><p>Request Method 为请求的方法</p></li>
<li><p>Status Code 为响应状态码</p></li>
<li><p>Remote Address 为远程服务器的地址和端口</p></li>
<li><p>Referrer Policy 为 Referrer 判别策略。</p></li>
</ul>
<p><strong>Response Headers</strong>——响应头</p>
<p>Response Headers其中包含了服务器的类型、文档类型、日期等信息，浏览器接受到响应后，会解析响应内容，进而呈现网页内容。</p>
<p><strong>Request Headers</strong>——请求头</p>
<p>请求头里带有许多请求信息，例如浏览器标识、Cookies、Host 等信息，这是请求的一部分，服务器会根据请求头内的信息判断请求是否合法，进而作出对应的响应。</p>
<h2 id="请求">请求</h2>
<p>请求，由客户端向服务端发出，可以分为 4 部分内容：请求方法（Request Method）、请求的网址（Request URL）、请求头（Request Headers）、请求体（Request Body）。</p>
<h3 id="请求方法">请求方法</h3>
<p>常见的请求方法有两种：<strong>GET</strong> 和 <strong>POST</strong>。</p>
<p><strong>GET方法</strong></p>
<p>在浏览器中直接输入 URL 并回车，这便发起了一个 GET 请求，请求的参数会直接包含到 URL 里。例如，在百度中搜索 Python，这就是一个 GET 请求，链接为 https://www.baidu.com/s?wd=Python，其中 URL 中包含了请求的参数信息，这里参数 wd 表示要搜寻的关键字。</p>
<p><strong>POST方法</strong></p>
<p>POST 请求大多在表单提交时发起。比如，对于一个登录表单，输入用户名和密码后，点击 “登录” 按钮，这通常会发起一个 POST 请求，其数据通常以表单的形式传输，而不会体现在 URL 中。</p>
<p>GET 和 POST 请求方法有如下区别。</p>
<ul>
<li>GET 请求中的参数包含在 URL 里面，数据可以在 URL 中看到，而 POST 请求的 URL 不会包含这些数据，数据都是通过表单形式传输的，会包含在请求体中。</li>
<li>GET 请求提交的数据最多只有 1024 字节，而 POST 请求没有限制。</li>
</ul>
<p>一般来说，登录时，需要提交用户名和密码，其中包含了敏感信息，使用 GET 方式请求的话，密码就会暴露在 URL 里面，造成密码泄露，所以这里最好以 POST 方式发送。上传文件时，由于文件内容比较大，也会选用 POST 方式。</p>
<p>我们平常遇到的绝大部分请求都是 GET 或 POST 请求，另外还有一些请求方法，如 HEAD、PUT、DELETE、OPTIONS、CONNECT、TRACE 等，我们简单将其总结为下表。</p>
<figure>
<img src="https://i.loli.net/2020/03/07/Cc718EeSt2nFHrm.png" alt="1-5.png" /><figcaption>1-5.png</figcaption>
</figure>
<p>请求的网址，即统一资源定位符 URL，它可以唯一确定我们想请求的资源。</p>
<h3 id="请求头">请求头</h3>
<p>请求头，用来说明服务器要使用的附加信息，比较重要的信息有 Cookie、Referer、User-Agent 等。下面简要说明一些常用的头信息。</p>
<ul>
<li><p><strong>Cookie</strong>：也常用复数形式 Cookies，这是网站为了辨别用户进行会话跟踪而存储在用户本地的数据。它的主要功能是维持当前访问会话。例如，我们输入用户名和密码成功登录某个网站后，服务器会用会话保存登录状态信息，后面我们每次刷新或请求该站点的其他页面时，会发现都是登录状态，这就是 Cookies 的功劳。Cookies 里有信息标识了我们所对应的服务器的会话，每次浏览器在请求该站点的页面时，都会在请求头中加上 Cookies 并将其发送给服务器，服务器通过 Cookies 识别出是我们自己，并且查出当前状态是登录状态，所以返回结果就是登录之后才能看到的网页内容。</p></li>
<li><p><strong>Referer</strong>：此内容用来标识这个请求是从哪个页面发过来的，服务器可以拿到这一信息并做相应的处理，如做来源统计、防盗链处理等。</p></li>
<li><p><strong>User-Agent</strong>：简称 UA，它是一个特殊的字符串头，可以使服务器识别客户使用的操作系统及版本、浏览器及版本等信息。在做爬虫时加上此信息，可以伪装为浏览器；如果不加，很可能会被识别出为爬虫。</p>
<h4 id="其他请求头信息">其他请求头信息</h4></li>
<li><p>Accept：请求报头域，用于指定客户端可接受哪些类型的信息。</p></li>
<li><p>Accept-Language：指定客户端可接受的语言类型。</p></li>
<li><p>Accept-Encoding：指定客户端可接受的内容编码。</p></li>
<li><p>Host：用于指定请求资源的主机 IP 和端口号，其内容为请求 URL 的原始服务器或网关的位置。从 HTTP 1.1 版本开始，请求必须包含此内容。</p></li>
<li><p>Content-Type：也叫互联网媒体类型（Internet Media Type）或者 MIME 类型，在 HTTP 协议消息头中，它用来表示具体请求中的媒体类型信息。例如，text/html 代表 HTML 格式，image/gif 代表 GIF 图片，application/json 代表 JSON 类型，更多对应关系可以查看此对照表：http://tool.oschina.net/commons。</p></li>
</ul>
<p>因此，请求头是请求的重要组成部分，在写爬虫时，大部分情况下都需要设定请求头。</p>
<h3 id="请求体">请求体</h3>
<p>请求体一般承载的内容是 POST 请求中的表单数据，而对于 GET 请求，请求体则为空。</p>
<h2 id="响应">响应</h2>
<p>响应，由服务端返回给客户端，可以分为三部分：响应状态码（Response Status Code）、响应头（Response Headers）和响应体（Response Body）。</p>
<h3 id="响应状态码">响应状态码</h3>
<p>响应状态码表示服务器的响应状态，如 200 代表服务器正常响应，404 代表页面未找到，500 代表服务器内部发生错误。在爬虫中，我们可以根据状态码来判断服务器响应状态，如状态码为 200，则证明成功返回数据，再进行进一步的处理，否则直接忽略。</p>
<h3 id="响应头">响应头</h3>
<p>包含了服务器对请求的应答信息，如 Content-Type、Server、Set-Cookie 等。下面简要说明一些常用的头信息。</p>
<ul>
<li>Date：标识响应产生的时间。</li>
<li>Last-Modified：指定资源的最后修改时间。</li>
<li>Content-Encoding：指定响应内容的编码。</li>
<li>Server：包含服务器的信息，比如名称、版本号等。</li>
<li>Content-Type：文档类型，指定返回的数据类型是什么，如 text/html 代表返回 HTML 文档，application/x-javascript 则代表返回 JavaScript 文件，image/jpeg 则代表返回图片。</li>
<li>Set-Cookie：设置 Cookies。响应头中的 Set-Cookie 告诉浏览器需要将此内容放在 Cookies 中，下次请求携带 Cookies 请求。</li>
<li>Expires：指定响应的过期时间，可以使代理服务器或浏览器将加载的内容更新到缓存中。如果再次访问时，就可以直接从缓存中加载，降低服务器负载，缩短加载时间。</li>
</ul>
<h3 id="响应体">★响应体</h3>
<p>最重要的当属响应体的内容了。响应的正文数据都在响应体中，比如请求网页时，它的响应体就是网页的 HTML 代码；请求一张图片时，它的响应体就是图片的二进制数据。我们做爬虫请求网页后，要解析的内容就是响应体，如图所示。</p>
<p>在浏览器开发者工具中点击 Preview，就可以看到网页的源代码，也就是响应体的内容，它是解析的目标。</p>
<p>在做爬虫时，我们主要通过响应体得到网页的源代码、JSON 数据等，然后从中做相应内容的提取。</p>
]]></content>
      <categories>
        <category>爬虫</category>
      </categories>
      <tags>
        <tag>tutorial</tag>
        <tag>爬虫</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习入门</title>
    <url>/2020/ck7mctjgu000if4vshocx9dpo/</url>
    <content><![CDATA[<h2 id="kaggle官方教程之机器学习入门">Kaggle官方教程之机器学习入门</h2>
<p>[toc]</p>
<hr />
<h3 id="模型如何工作机器学习入门介绍">1.模型如何工作？——机器学习入门介绍</h3>
<h4 id="简介">1.1 简介</h4>
<p>首先，我们将简述机器学习模型如何工作以及如何使用模型。 如果您之前已经进行过统计建模或机器学习，您可能会觉得很基础。 不用担心，我们在后面的课程建立更加强大的模型。学习此微课程，您可以在以下场景构建模型：</p>
<p>你的堂兄搞房地产赚了数百万美元。 由于你对数据科感兴趣兴趣，他想和你成为业务合作伙伴。 他将提供资金，并且您将提供预测各种房屋价值的模型。你问你的堂兄，他过去是如何预测房地产价值。 他说这只是直觉。 然而你认为，他从过去的房屋中识别出了价格的模式(pattern)，然后他利用这些模式对他正在考虑的新房屋做出预测。</p>
<a id="more"></a>
<p>机器学习的工作方式相同。 我们将从称为决策树的模型开始。 有更高级的模型可以提供更准确的预测。 但是决策树很容易理解，它们是数据科学中一些最佳模型的基本构建块。为简单起见，我们将从最简单的决策树开始。</p>
<figure>
<img src="https://i.loli.net/2020/03/07/rLgERAwiINDBXvc.png" alt="7tsb5b1.png" /><figcaption>7tsb5b1.png</figcaption>
</figure>
<p>它仅将房屋分为两类。 房屋的预测价格为同一类别房屋的历史平均价格。我们使用数据来决定如何将房屋分为两组，然后再次确定每组中的预测价格。 从数据获取模式的这一步骤称为<strong>拟合</strong>或<strong>训练</strong>模型。 用于拟合模型的数据称为<strong>训练数据</strong>。</p>
<p>有关模型拟合方式的详细信息（例如，如何拆分数据）非常复杂，我们将在以后考虑。 模型在训练之后，可以将其应用于新数据来预测其他房屋的价格。</p>
<h4 id="改进决策树">1.2 改进决策树</h4>
<p>通过拟合房地产训练数据，以下两个决策树中的哪个更有可能产生？</p>
<figure>
<img src="https://i.loli.net/2020/03/07/vDTtmPqRQEbjfNx.png" alt="prAjgku.png" /><figcaption>prAjgku.png</figcaption>
</figure>
<p>左侧的决策树可能更有意义，因为它反映了一个现实，即卧室多的房屋比卧室少的房屋的价格更高。 该模型的最大缺点是，它没有涵盖更多影响房价的因素，例如浴室数量，地段大小，位置等。</p>
<p>您可以使用具有更多“分支”的树来获取更多因素。 这些被称为“更深”的树。 还要考虑每栋房屋总面积的决策树可能如下所示：</p>
<figure>
<img src="https://i.loli.net/2020/03/07/eORHfbyQKCVBcD6.png" alt="R3ywQsR.png" /><figcaption>R3ywQsR.png</figcaption>
</figure>
<p>您可以通过跟踪决策树来预测任何房屋的价格，并始终选择与房屋特征相对应的路径。 房屋的预计价格在树的底部。 我们进行预测的底部点称为叶子节点。</p>
<p>叶子上的分支和值将由数据确定，因此是时候检查要使用的数据了。</p>
<hr />
<h3 id="基础数据探索载入理解数据">2.基础数据探索——载入理解数据</h3>
<h4 id="使用pandas了解你的数据">2.1 使用Pandas了解你的数据</h4>
<p>任何机器学习项目的第一步都是熟悉数据。 你将使用Pandas库。 Pandas是用来探索和处理数据的主要工具。 大多数人在其代码中将pandas缩写为pd。 我们使用命令执行此操作</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br></pre></td></tr></table></figure>
<p>Pandas库中最重要的部分是DataFrame。DataFrame包含可以看作是表的数据类型。 这类似于Excel中的工作表或SQL数据库中的表。Pandas具有强大的方法，可用于处理此类数据。例如，我们将查看澳大利亚墨尔本的房价数据。 在动手练习中，您将对新数据集采用相同的过程，该数据集是爱荷华州的房价。</p>
<p>示例（墨尔本）数据位于文件路径<code>../input/melbourne-housing-snapshot/melb_data.csv</code></p>
<p>我们使用以下命令加载和浏览数据：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#为了便于获取，将文件路径保存到变量中</span></span><br><span class="line">melbourne_file_path=<span class="string">'../input/melbourne-housing-snapshot/melb_data.csv'</span></span><br><span class="line"><span class="comment">#读取数据，将数据存储到DataFrame中</span></span><br><span class="line">melbourne_data=pd.read_csv(melbourne_file_path)</span><br><span class="line"><span class="comment">#打印melbourne_data的数据情况</span></span><br><span class="line">melbourne_data.describe()</span><br></pre></td></tr></table></figure>
<table>
<colgroup>
<col style="width: 3%" />
<col style="width: 7%" />
<col style="width: 7%" />
<col style="width: 7%" />
<col style="width: 7%" />
<col style="width: 7%" />
<col style="width: 7%" />
<col style="width: 7%" />
<col style="width: 8%" />
<col style="width: 7%" />
<col style="width: 6%" />
<col style="width: 7%" />
<col style="width: 8%" />
<col style="width: 7%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">Rooms</th>
<th style="text-align: left;">Price</th>
<th style="text-align: left;">Distance</th>
<th style="text-align: left;">Postcode</th>
<th style="text-align: left;">Bedroom2</th>
<th style="text-align: left;">Bathroom</th>
<th style="text-align: left;">Car</th>
<th style="text-align: left;">Landsize</th>
<th style="text-align: left;">BuildingArea</th>
<th style="text-align: left;">YearBuilt</th>
<th style="text-align: left;">Lattitude</th>
<th style="text-align: left;">Longtitude</th>
<th style="text-align: left;">Propertycount</th>
<th></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;">count</td>
<td style="text-align: left;">13580.000000</td>
<td style="text-align: left;">1.358000e+04</td>
<td style="text-align: left;">13580.000000</td>
<td style="text-align: left;">13580.000000</td>
<td style="text-align: left;">13580.000000</td>
<td style="text-align: left;">13580.000000</td>
<td style="text-align: left;">13518.000000</td>
<td style="text-align: left;">13580.000000</td>
<td style="text-align: left;">7130.000000</td>
<td style="text-align: left;">8205.000000</td>
<td style="text-align: left;">13580.000000</td>
<td style="text-align: left;">13580.000000</td>
<td>13580.000000</td>
</tr>
<tr class="even">
<td style="text-align: left;">mean</td>
<td style="text-align: left;">2.937997</td>
<td style="text-align: left;">1.075684e+06</td>
<td style="text-align: left;">10.137776</td>
<td style="text-align: left;">3105.301915</td>
<td style="text-align: left;">2.914728</td>
<td style="text-align: left;">1.534242</td>
<td style="text-align: left;">1.610075</td>
<td style="text-align: left;">558.416127</td>
<td style="text-align: left;">151.967650</td>
<td style="text-align: left;">1964.684217</td>
<td style="text-align: left;">-37.809203</td>
<td style="text-align: left;">144.995216</td>
<td>7454.417378</td>
</tr>
<tr class="odd">
<td style="text-align: left;">std</td>
<td style="text-align: left;">0.955748</td>
<td style="text-align: left;">6.393107e+05</td>
<td style="text-align: left;">5.868725</td>
<td style="text-align: left;">90.676964</td>
<td style="text-align: left;">0.965921</td>
<td style="text-align: left;">0.691712</td>
<td style="text-align: left;">0.962634</td>
<td style="text-align: left;">3990.669241</td>
<td style="text-align: left;">541.014538</td>
<td style="text-align: left;">37.273762</td>
<td style="text-align: left;">0.079260</td>
<td style="text-align: left;">0.103916</td>
<td>4378.581772</td>
</tr>
<tr class="even">
<td style="text-align: left;">min</td>
<td style="text-align: left;">1.000000</td>
<td style="text-align: left;">8.500000e+04</td>
<td style="text-align: left;">0.000000</td>
<td style="text-align: left;">3000.000000</td>
<td style="text-align: left;">0.000000</td>
<td style="text-align: left;">0.000000</td>
<td style="text-align: left;">0.000000</td>
<td style="text-align: left;">0.000000</td>
<td style="text-align: left;">0.000000</td>
<td style="text-align: left;">1196.000000</td>
<td style="text-align: left;">-38.182550</td>
<td style="text-align: left;">144.431810</td>
<td>249.000000</td>
</tr>
<tr class="odd">
<td style="text-align: left;">25%</td>
<td style="text-align: left;">2.000000</td>
<td style="text-align: left;">6.500000e+05</td>
<td style="text-align: left;">6.100000</td>
<td style="text-align: left;">3044.000000</td>
<td style="text-align: left;">2.000000</td>
<td style="text-align: left;">1.000000</td>
<td style="text-align: left;">1.000000</td>
<td style="text-align: left;">177.000000</td>
<td style="text-align: left;">93.000000</td>
<td style="text-align: left;">1940.000000</td>
<td style="text-align: left;">-37.856822</td>
<td style="text-align: left;">144.929600</td>
<td>4380.000000</td>
</tr>
<tr class="even">
<td style="text-align: left;">50%</td>
<td style="text-align: left;">3.000000</td>
<td style="text-align: left;">9.030000e+05</td>
<td style="text-align: left;">9.200000</td>
<td style="text-align: left;">3084.000000</td>
<td style="text-align: left;">3.000000</td>
<td style="text-align: left;">1.000000</td>
<td style="text-align: left;">2.000000</td>
<td style="text-align: left;">440.000000</td>
<td style="text-align: left;">126.000000</td>
<td style="text-align: left;">1970.000000</td>
<td style="text-align: left;">-37.802355</td>
<td style="text-align: left;">145.000100</td>
<td>6555.000000</td>
</tr>
<tr class="odd">
<td style="text-align: left;">75%</td>
<td style="text-align: left;">3.000000</td>
<td style="text-align: left;">1.330000e+06</td>
<td style="text-align: left;">13.000000</td>
<td style="text-align: left;">3148.000000</td>
<td style="text-align: left;">3.000000</td>
<td style="text-align: left;">2.000000</td>
<td style="text-align: left;">2.000000</td>
<td style="text-align: left;">651.000000</td>
<td style="text-align: left;">174.000000</td>
<td style="text-align: left;">1999.000000</td>
<td style="text-align: left;">-37.756400</td>
<td style="text-align: left;">145.058305</td>
<td>10331.000000</td>
</tr>
<tr class="even">
<td style="text-align: left;">max</td>
<td style="text-align: left;">10.000000</td>
<td style="text-align: left;">9.000000e+06</td>
<td style="text-align: left;">48.100000</td>
<td style="text-align: left;">3977.000000</td>
<td style="text-align: left;">20.000000</td>
<td style="text-align: left;">8.000000</td>
<td style="text-align: left;">10.000000</td>
<td style="text-align: left;">433014.000000</td>
<td style="text-align: left;">44515.000000</td>
<td style="text-align: left;">2018.000000</td>
<td style="text-align: left;">-37.408530</td>
<td style="text-align: left;">145.526350</td>
<td>21650.000000</td>
</tr>
</tbody>
</table>
<p>结果显示了8个数字对应原始数据集中的每列。</p>
<p>第一个数字（count）显示有多少行具有非缺失值。数据缺失的原因很多。 例如，一居室的房子不会收集第二间卧室的大小。 我们将会在后面讨论缺失数据的问题。</p>
<p>第二个值是mean，即平均值。下面的std是标准偏差，它测量数值的分散程度。</p>
<p>min，25％，50％，75％和max，请想象把原始数据的每列进行从小到大排序。 第一个（最小）值是min。 如果您遍历列表的四分之一，则会发现一个数字，它比25％的数据大，比75％的数据小，这就是25％的值。 第50个百分位数和第75个百分位数是类似定义的。</p>
<h4 id="练习探索你的数据">2.2 练习：探索你的数据</h4>
<p>本练习将锻炼读取数据文件和了解有关数据的统计信息的能力。在以后的练习中，您将使用其他技术来过滤数据，建立机器学习模型并迭代地改进模型。上述教程中使用了墨尔本的数据作为实例。 为了确保您掌握了这些技巧，您需要将它们应用到新的数据集（爱荷华州的房价）中。</p>
<h5 id="步骤1载入数据">步骤1：载入数据</h5>
<p>将爱荷华州数据文件导入到DataFrame中，叫做home_data</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line">iowa_file_path=<span class="string">'../input/home-data-for-ml-course/train.csv'</span></span><br><span class="line">home_data=pd.read_csv(iowa_file_path)</span><br></pre></td></tr></table></figure>
<h5 id="步骤2查看数据">步骤2：查看数据</h5>
<p>使用您学到的命令来查看数据的摘要统计信息。 然后填写变量以回答以下问题</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">home_data.describe()</span><br></pre></td></tr></table></figure>
<h3 id="第一个机器学习模型从决策树开始">3.第一个机器学习模型——从决策树开始</h3>
<h4 id="选择建立模型所需的数据">3.1 选择建立模型所需的数据</h4>
<p>数据集包含太多变量，以至于无法很好地打印出来。 如何将大量的数据缩减为您可以理解的数据？作为开始，我们将从使用直觉选择一些变量。 以后的课程将向您展示自动确定变量优先级的统计技术。要选择变量/列，我们需要查看数据集中<strong>所有列的列表</strong>。 这是通过DataFrame的<strong>columns属性</strong>完成的（下面的代码底行）。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"></span><br><span class="line">melbourne_file_path = <span class="string">'../input/melbourne-housing-snapshot/melb_data.csv'</span></span><br><span class="line">melbourne_data = pd.read_csv(melbourne_file_path) </span><br><span class="line">melbourne_data.columns</span><br></pre></td></tr></table></figure>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#墨尔本数据缺少一些值（一些房屋未记录一些变量。）</span></span><br><span class="line"><span class="comment">#我们将在以后的教程中学习处理缺失值。</span></span><br><span class="line"><span class="comment">#您的爱荷华州数据在使用的列中没有缺少值。</span></span><br><span class="line"><span class="comment">#因此，我们现在将采用最简单的选项，并从数据中删除房屋。</span></span><br><span class="line"><span class="comment">#现在的代码是：</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#dropna删除缺少的值（认为na为“不可用”）</span></span><br><span class="line">melbourne_data = melbourne_data.dropna(axis = <span class="number">0</span>)</span><br></pre></td></tr></table></figure>
<p>有很多方法可以选择数据集的子集。 pandas微课程将更深入地介绍这些内容，目前我们将重点放在两种方法。</p>
<ul>
<li><strong>点表示法</strong>，用于选择<strong>“预测目标”</strong></li>
</ul>
<p>您可以使用点符号提取变量。 此单列存储在Series中，这在很大程度上类似于仅包含一列数据的DataFrame。我们将使用点表示法选择要预测的列，称为预测目标。 按照惯例，预测目标称为y。 因此，我们需要在墨尔本数据中保存房价的代码是</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">y = melbourne_data.Price</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>选择列的列表</strong>，我们用它来选择<strong>“特征”</strong></li>
</ul>
<p>输入到我们的模型中的列（以后用于预测）称为“features”。 在我们的案例中，这些t特征是用于确定房屋价格的列。 有时，您会将除目标以外的所有列用作features。 有时，更少的features会更好。现在，我们将构建仅具有一些功能的模型。 稍后，您将看到如何迭代和比较使用不同功能构建的模型。我们通过在方括号内提供列名称列表来选择多个功能。 该列表中的每个项目都应该是一个字符串（带引号）。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">melbourne_features = [<span class="string">'Rooms'</span>, <span class="string">'Bathroom'</span>, <span class="string">'Landsize'</span>, <span class="string">'Lattitude'</span>, <span class="string">'Longtitude'</span>]</span><br></pre></td></tr></table></figure>
<p>为了方便起见，把数据叫做<span class="math inline">\(x\)</span></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">X = melbourne_data[melbourne_features]</span><br></pre></td></tr></table></figure>
<p>让我们使用describe方法和head方法快速查看将用于预测房价的数据，该方法显示数据的前几行。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">X.describe()</span><br><span class="line">X.head()</span><br></pre></td></tr></table></figure>
<p>使用这些命令直观地检查数据是数据科学家工作的重要组成部分。 您会经常在数据集中发现需要进一步检查的意外情况.</p>
<h4 id="建立模型">3.2 建立模型</h4>
<p>您将使用scikit-learn库创建模型。 如示例代码所示，该库以sklearn编写。 Scikit-learn是最受欢迎的库，它用于对通常存储在DataFrames中的数据类型进行建模。</p>
<p>建立和使用模型的步骤是：</p>
<p>定义：哪种类型的模型？ 决策树？ 其他类型的模型？</p>
<p>训练：从提供的数据中获取模式。 这是建模的核心。</p>
<p>预测：对新的数据进行预测</p>
<p>评估：确定模型预测的准确性。</p>
<p>如下所示是一个使用scikit-learn的决策树模型，训练模型让并特征和目标变量相匹配。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeRegressor</span><br><span class="line">melbourne_model=DecisionTreeRegressor(random_state=<span class="number">1</span>)</span><br><span class="line">melbourne_model.fit(X,y)</span><br></pre></td></tr></table></figure>
<p>许多机器学习模型在模型训练中允许一些随机性。 为random_state指定一个数字可确保您在每次运行中获得相同的结果。 这是一个好习惯。 您可以使用任何数字，并且模型的质量在很大程度上不会取决于您选择的值。</p>
<p>现在，我们有了一个拟合模型，可以用来进行预测。</p>
<p>实际上，您将要对市场上即将出现的新房子而不是我们已经有价格的房子做出预测。 但是，我们将对训练数据的前几行进行预测，以了解预测函数的工作方式。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">print(<span class="string">"Making predictions for the following 5 houses:"</span>)</span><br><span class="line">print(X.head())</span><br><span class="line">print(<span class="string">"预测值："</span>)</span><br><span class="line">print(melbourne_model.predict(X.head()))</span><br></pre></td></tr></table></figure>
<h4 id="练习">3.3 练习</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Code you have previously used to load data</span></span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"></span><br><span class="line"><span class="comment"># Path of the file to read</span></span><br><span class="line">iowa_file_path = <span class="string">'../input/home-data-for-ml-course/train.csv'</span></span><br><span class="line"></span><br><span class="line">home_data = pd.read_csv(iowa_file_path)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Set up code checking</span></span><br><span class="line"><span class="keyword">from</span> learntools.core <span class="keyword">import</span> binder</span><br><span class="line">binder.bind(globals())</span><br><span class="line"><span class="keyword">from</span> learntools.machine_learning.ex3 <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">print(<span class="string">"Setup Complete"</span>)</span><br></pre></td></tr></table></figure>
<h5 id="步骤1指定预测目标">步骤1：指定预测目标</h5>
<p>选择与销售价格相对应的目标变量。 将其保存到名为“ y”的新变量中。 您需要打印列的列表以查找所需列的名称。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">home_data.columns</span><br><span class="line">y=home_data[<span class="string">'SalePrice'</span>]</span><br></pre></td></tr></table></figure>
<h5 id="步骤2创建特征向量x">步骤2：创建特征向量X</h5>
<p>现在，您将创建一个名为“ X”的数据框，其中包含预测特征。由于只需要原始数据中的某些列，因此首先要创建一个列表，其中包含要在<code>X</code>中使用的列的名称。</p>
<p>您将只使用列表中的以下列（您可以复制并粘贴整个列表以保存一些输入内容，尽管您仍然需要添加引号）：</p>
<p>LotArea</p>
<p>YearBuilt</p>
<p>1stFlrSF</p>
<p>2ndFlrSF</p>
<p>FullBath</p>
<p>BedroomAbvGr</p>
<p>TotRmsAbvGrd</p>
<p>创建该特征列表之后，使用它来创建用于拟合模型的DataFrame。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Create the list of features below</span></span><br><span class="line">feature_names = [<span class="string">"LotArea"</span>,<span class="string">"YearBuilt"</span>,<span class="string">"1stFlrSF"</span>,<span class="string">"2ndFlrSF"</span>,<span class="string">"FullBath"</span>,<span class="string">"BedroomAbvGr"</span>,<span class="string">"TotRmsAbvGrd"</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># Select data corresponding to features in feature_names</span></span><br><span class="line">X = home_data[feature_names]</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_2.check()</span><br></pre></td></tr></table></figure>
<p><strong>查看数据</strong></p>
<p>在建立模型之前，快速浏览一下<strong>X</strong>以验证它是否合理</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Review data</span></span><br><span class="line"><span class="comment"># print description or statistics from X</span></span><br><span class="line">print(X.describe())</span><br><span class="line"></span><br><span class="line"><span class="comment"># print the top few lines</span></span><br><span class="line">print(X.head())</span><br></pre></td></tr></table></figure>
<h5 id="步骤3指定并拟合模型">步骤3：指定并拟合模型</h5>
<p>创建一个<code>DecisionTreeRegressor</code>并将其保存为iowa_model。 确保已完成从sklearn的相关导入以运行此命令。</p>
<p>然后使用上面保存的<code>X</code>和<code>y</code>中的数据拟合刚刚创建的模型。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeRegressor</span><br><span class="line"><span class="comment">#specify the model. </span></span><br><span class="line"><span class="comment">#For model reproducibility, set a numeric value for random_state when specifying the model</span></span><br><span class="line">iowa_model = DecisionTreeRegressor(random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Fit the model</span></span><br><span class="line">iowa_model.fit(X,y)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_3.check()</span><br></pre></td></tr></table></figure>
<h5 id="步骤4做出预测">步骤4：做出预测</h5>
<p>使用<code>X</code>数据，使用模型的<code>predict</code>命令进行预测。 将结果保存到名为“预测”的变量中。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">predictions = iowa_model.predict(X)</span><br><span class="line">print(predictions)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_4.check()</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="模型验证如何评价模型的性能">4.模型验证——如何评价模型的性能？</h3>
<p>您已经建立了一个模型，如何评价模型的性能高低？</p>
<p>在本课程中，您将学习使用模型验证来度量模型的质量。 测量模型质量是迭代改进模型的关键。</p>
<h4 id="什么是模型验证">4.1 什么是模型验证</h4>
<p>要评估几乎所有构建的模型模型。 在大多数（尽管不是全部）应用中，模型质量的评价标准是预测的准确性。换句话说，模型的预测接近实际发生的情况的可能性。</p>
<p>总结模型质量有很多指标，我们将从一个称为平均绝对误差（Mean Absolute Error，MAE）的指标开始。 让我们从最后一个词“Error”开始分解这个指标。</p>
<p>每个房屋的预测误差为：</p>
<p><code>error=actual-predicted</code></p>
<p>因此，如果一所房屋的价格为$ 150,000，而您预测这将花费$ 100,000，则错误为$ 50,000。</p>
<p>使用MAE度量，我们可以获取每个误差的绝对值。 这会将每个错误转换为正数。 然后，我们取这些绝对误差的平均值。 这是我们对模型质量的衡量。要计算MAE，我们首先需要一个模型。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Data Loading Code Hidden Here</span></span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"></span><br><span class="line"><span class="comment"># Load data</span></span><br><span class="line">melbourne_file_path = <span class="string">'../input/melbourne-housing-snapshot/melb_data.csv'</span></span><br><span class="line">melbourne_data = pd.read_csv(melbourne_file_path) </span><br><span class="line"><span class="comment"># Filter rows with missing price values</span></span><br><span class="line">filtered_melbourne_data = melbourne_data.dropna(axis=<span class="number">0</span>)</span><br><span class="line"><span class="comment"># Choose target and features</span></span><br><span class="line">y = filtered_melbourne_data.Price</span><br><span class="line">melbourne_features = [<span class="string">'Rooms'</span>, <span class="string">'Bathroom'</span>, <span class="string">'Landsize'</span>, <span class="string">'BuildingArea'</span>, </span><br><span class="line">                        <span class="string">'YearBuilt'</span>, <span class="string">'Lattitude'</span>, <span class="string">'Longtitude'</span>]</span><br><span class="line">X = filtered_melbourne_data[melbourne_features]</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DecisionTreeRegressor</span><br><span class="line"><span class="comment"># Define model</span></span><br><span class="line">melbourne_model = DecisionTreeRegressor()</span><br><span class="line"><span class="comment"># Fit model</span></span><br><span class="line">melbourne_model.fit(X, y)</span><br></pre></td></tr></table></figure>
<p>一旦有了模型，就可以计算平均绝对误差：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_absolute_error</span><br><span class="line">predicted_home_prices=melbourne_model.predicted(X)</span><br><span class="line">mean_absolute_error(y,predicted_home_prices)</span><br></pre></td></tr></table></figure>
<h4 id="样本内得分">4.2 “样本内”得分</h4>
<p>我们刚刚计算出的度量可以称为“样本内”得分。我们使用单个房屋“样本”来构建模型和评估模型。</p>
<p>为什么“样本内”得分不好呢？想象一下，在大型房地产市场中，门的颜色与房价无关。但是，在用于构建模型的数据样本中，所有带有绿色门的房屋都非常昂贵。该模型的工作是找到可以预测房价的模式，因此它将看到此模式，并且始终可以预测带有绿色门的房屋的高价格。由于此模式是从训练数据中得出的，因此模型在训练数据中将显得准确。但是，如果在模型看到新数据时这种模式不成立，那么在实践中使用该模型将非常不准确。</p>
<p>由于模型的实际价值来自对新数据的预测，因此我们可以评估未用于构建模型的数据的性能。最简单的方法是从模型构建过程中排除一些数据，然后使用这些数据对以前从未见过的数据测试模型的准确性。该数据称为验证数据。</p>
<p><strong>代码实现</strong> scikit-learn库具有函数train_test_split，可将数据分为两部分。 我们将使用其中一些数据作为适合模型的训练数据，并使用其他数据作为验证数据来计算mean_absolute_error。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="comment">#将数据分为训练集和验证集</span></span><br><span class="line"><span class="comment">#拆分基于随机数生成器。 提供一个数值给random_state参数保证每次我们得到相同的分割</span></span><br><span class="line">train_X,val_X,train_Y,val_Y=train_test_split(X,y,random_state=<span class="number">0</span>)</span><br><span class="line"><span class="comment">#定义模型</span></span><br><span class="line">melbourne_model = DecisionTreeRegressor()</span><br><span class="line"><span class="comment">#训练模型</span></span><br><span class="line">melbourne_model.fit(train_X,train_Y)</span><br><span class="line"><span class="comment">#根据验证数据获得预测房价</span></span><br><span class="line">val_predictions=melbourne_model.predict(val_X)</span><br><span class="line">print(mean_absolute_error(val_Y,val_predictions))</span><br></pre></td></tr></table></figure>
<h4 id="练习-1">4.4 练习</h4>
<h5 id="步骤1拆分数据">步骤1：拆分数据</h5>
<p>使用<code>train_test_split</code>函数来分割数据。 给它加上参数<code>random_state = 1</code>，以便<code>check</code>函数知道验证代码时的期望。 回想一下，您的功能已加载到DataFrame <strong>X</strong>中，而目标已加载到<strong>y</strong>中</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Import the train_test_split function and uncomment</span></span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"></span><br><span class="line"><span class="comment"># fill in and uncomment</span></span><br><span class="line">train_X, val_X, train_y, val_y = train_test_split(X,y,random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_1.check()</span><br></pre></td></tr></table></figure>
<h5 id="步骤2指定并拟合模型">步骤2：指定并拟合模型</h5>
<p>创建一个“ DecisionTreeRegressor”模型并将其拟合到相关数据。 建立模型时，再次random_state设为1</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Specify the model</span></span><br><span class="line">iowa_model = DecisionTreeRegressor(random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Fit iowa_model with the training data.</span></span><br><span class="line">iowa_model.fit(train_X,train_y)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_2.check()</span><br></pre></td></tr></table></figure>
<h5 id="步骤3使用验证数据进行预测">步骤3：使用验证数据进行预测</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Predict with all validation observations</span></span><br><span class="line">val_predictions = iowa_model.predict(val_X)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_3.check()</span><br></pre></td></tr></table></figure>
<p>检查来自验证数据的预测和实际值。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># print the top few validation predictions</span></span><br><span class="line">print(val_predictions[:<span class="number">5</span>])</span><br><span class="line"><span class="comment"># print the top few actual prices from validation data</span></span><br><span class="line">print(val_y[:<span class="number">5</span>])</span><br></pre></td></tr></table></figure>
<h5 id="步骤4计算验证数据中的平均绝对误差">步骤4：计算验证数据中的平均绝对误差</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_absolute_error</span><br><span class="line">val_mae = mean_absolute_error(val_predictions,val_y)</span><br><span class="line"></span><br><span class="line"><span class="comment"># uncomment following line to see the validation_mae</span></span><br><span class="line">print(val_mae)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_4.check()</span><br></pre></td></tr></table></figure>
<p>那MAE好吗？对于适用于整个应用程序的值没有一个通用规则。但是，您将在下一步中了解如何使用（和改进）此数字。</p>
<hr />
<h3 id="欠拟合和过拟合微调模型以获得更好的性能">5.欠拟合和过拟合——微调模型以获得更好的性能</h3>
<p>在此步骤的最后，您将了解过拟合和过拟合的概念，并应用这些概念来使模型更加准确。</p>
<h4 id="实验不同的模型">5.1 实验不同的模型</h4>
<p>现在，您已经有了一种可靠的方法来衡量模型的准确性，您可以尝试使用替代模型，看看哪种模型可以提供最佳的预测。有哪些模型可供选择呢？</p>
<p>您可以在scikit-learn的文档中看到，决策树模型具有许多选项（其数量远远超过您在未来一段时间里需要用的选项数量）。其中，最重要的选项是确定树的深度。回想一下这个微课程的第一课，一棵树的深度是对树木进行预测之前进行多少次分裂的度量。</p>
<p><a href="https://scikit-learn.org/stable/modules/generated/sklearn.tree.DecisionTreeRegressor.html" target="_blank" rel="noopener">sklearn.tree.DecisionTreeRegressor参数介绍</a></p>
<p>这是一棵深度为2的树:</p>
<figure>
<img src="../../../../MARKDOWN/kaggle-courses/机器学习入门教程/R3ywQsR.png" alt="R3ywQsR" /><figcaption>R3ywQsR</figcaption>
</figure>
<p>实际上，一棵树在顶层（所有房屋）和叶子之间有10个拆分并不少见。随着树变深，数据集被切成叶子，房屋更少。如果一棵树只有1个拆分，它将把数据分成2组。如果每组再次拆分，我们将获得4组房屋。再次拆分每个将创建8个组。如果我们通过在每个级别添加更多的拆分来使组的数量增加一倍，那么到第10级时，我们将拥有<span class="math inline">\(2^{10}\)</span>个房屋组。那是1024片叶子。</p>
<p>当我们在许多叶子之间划分房屋时，每个叶子中的房屋也更少。拥有很少房屋的叶子进行的预测将非常接近那些房屋的实际值，但是它们对新数据的预测可能非常不可靠（因为每个预测仅基于少数房屋）。这是一种称为过度拟合的现象，其中模型几乎完美地匹配了训练数据，但在验证和其他新数据方面却表现不佳。另一方面，如果我们使树非常浅，则不会将房屋分成非常不同的组。</p>
<p>在极端情况下，如果一棵树将房屋仅分为2或4，则每组仍然拥有各种各样的房屋。即使对于训练数据，大多数房屋的预测结果也可能相去甚远（由于相同的原因，其验证也很糟糕）。当模型无法捕获数据中的重要区别和模式时，即使在训练数据中它也表现不佳，这被称为欠拟合。</p>
<p>由于我们关心的是根据验证数据估算出的新数据的准确性，因此我们希望找到欠拟合与过度拟合之间的最佳结合点。在视觉上，我们希望得到（红色）验证曲线的最低点</p>
<figure>
<img src="../../../../MARKDOWN/kaggle-courses/机器学习入门教程/2q85n9s.png" alt="2q85n9s" /><figcaption>2q85n9s</figcaption>
</figure>
<h4 id="举例">5.2 举例</h4>
<p>有几种方法可以控制树的深度，许多方法允许通过树的某些路线的深度比其他路线的深度大。max_leaf_nodes参数提供了一种的方法来控制过拟合与欠拟合。模型的叶子越多，模型越远离欠拟合区域、越靠近过拟合区域。</p>
<p>我们可以使用一个效用函数来帮助比较max_leaf_nodes的不同值的MAE分数：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_absolut_error</span><br><span class="line"><span class="keyword">from</span> sklearn.tree <span class="keyword">import</span> DesicionTreeRegreesor</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_mea</span><span class="params">(max_leaf_nodes,train_X,val_X,train_y,val_y)</span>:</span></span><br><span class="line">    model=DesicionTreeRegressor(max_leaf_nodes=max_leaf_nodes,random_state=<span class="number">0</span>)</span><br><span class="line">    model.fit(train_X,train_7)</span><br><span class="line">    pred_val=model.predict(val_X)</span><br><span class="line">    mae=mean_absolute_error(val_y,pred_val)</span><br><span class="line">    <span class="keyword">return</span> mae</span><br></pre></td></tr></table></figure>
<p>使用上述代码将数据加载到train_X，val_X，train_，val_y中。</p>
<p>我们可以使用for循环比较使用max_leaf_nodes的不同值构建的模型的准确性。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> max_leaf_nodes <span class="keyword">in</span>[<span class="number">5</span>,<span class="number">50</span>,<span class="number">500</span>,<span class="number">5000</span>]:</span><br><span class="line">    my_mae=get_mae(max_leaf_nodes,train_X,val_X,train_y,val_y)</span><br><span class="line">    print(<span class="string">"最大叶子节点：%d \t \t MAE：%d"</span> %d(max_leaf_nodes,my_mae))</span><br></pre></td></tr></table></figure>
<h4 id="结论">5.3 结论</h4>
<p>以下两种情况都会导致模型预测不准确</p>
<p><strong>过拟合</strong>：捕获了将来不会再次出现的虚假模式，从而导致较不准确的预测。 <strong>欠拟合</strong>：未能捕获相关模式，导致预测不准确。 我们使用模型训练中未使用的<strong>验证数据</strong>来衡量候选模型的准确性。这使我们可以尝试许多候选模型并选择最优的模型。</p>
<h4 id="练习-2">5.4 练习</h4>
<p>您可以自己编写函数<code>get_mae</code>，也可以使用如下单元格中我们提供的函数。这与您在上一课中了解的功能相同。只需运行下面的单元格。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_mae</span><span class="params">(max_leaf_nodes, train_X, val_X, train_y, val_y)</span>:</span></span><br><span class="line">    model = DecisionTreeRegressor(max_leaf_nodes=max_leaf_nodes, random_state=<span class="number">0</span>)</span><br><span class="line">    model.fit(train_X, train_y)</span><br><span class="line">    preds_val = model.predict(val_X)</span><br><span class="line">    mae = mean_absolute_error(val_y, preds_val)</span><br><span class="line">    <span class="keyword">return</span>(mae)</span><br></pre></td></tr></table></figure>
<h5 id="步骤1比较不同的树大小">步骤1：比较不同的树大小</h5>
<p>编写一个循环，尝试从一组可能的值中为<strong>max_leaf_nodes</strong>尝试以下值[5, 25, 50, 100, 250, 500]。</p>
<p>在max_leaf_nodes的每个值上调用<strong>get_mae</strong>函数。以某种方式存储输出，该方式允许您选择max_leaf_nodes的值，该值可为您的数据提供最准确的模型</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">candidate_max_leaf_nodes = [<span class="number">5</span>, <span class="number">25</span>, <span class="number">50</span>, <span class="number">100</span>, <span class="number">250</span>, <span class="number">500</span>]</span><br><span class="line"><span class="comment"># Write loop to find the ideal tree size from candidate_max_leaf_nodes</span></span><br><span class="line">mapp=[]</span><br><span class="line"><span class="keyword">for</span> max_leaf_nodes <span class="keyword">in</span> candidate_max_leaf_nodes:</span><br><span class="line">    mae=get_mae(max_leaf_nodes,train_X, val_X, train_y, val_y)</span><br><span class="line">    mapp.append([max_leaf_nodes,mae])</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">second</span><span class="params">(elem)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> elem[<span class="number">1</span>]</span><br><span class="line">mapp.sort(key=second)</span><br><span class="line"><span class="comment"># Store the best value of max_leaf_nodes (it will be either 5, 25, 50, 100, 250 or 500)</span></span><br><span class="line">best_tree_size = mapp[<span class="number">0</span>][<span class="number">0</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_1.check()</span><br></pre></td></tr></table></figure>
<p><del>这个答案是本菜写的，太垃圾了不够美丽，下面放出官方答案：</del></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">scores = &#123;leaf_size: get_mae(leaf_size, train_X, val_X, train_y, val_y) <span class="keyword">for</span> leaf_size <span class="keyword">in</span> candidate_max_leaf_nodes&#125;</span><br><span class="line">best_tree_size = min(scores, key=scores.get)</span><br></pre></td></tr></table></figure>
<h5 id="步骤2使用所有数据拟合模型">步骤2：使用所有数据拟合模型</h5>
<p>您知道了最佳的树大小。如果您打算在实践中部署此模型，您应该使用所有的数据，让预测模型更加精确。也就是说，既然已做出所有建模决策，则无需保留验证数据。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Fill in argument to make optimal size and uncomment</span></span><br><span class="line">final_model = DecisionTreeRegressor(max_leaf_nodes=best_tree_size,random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># fit the final model and uncomment the next two lines</span></span><br><span class="line">final_model.fit(X, y)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_2.check()</span><br></pre></td></tr></table></figure>
<p>您已经调整了该模型并改善了结果。但是我们仍在使用决策树模型，这在现代机器学习标准中并不复杂。在下一步中，您将学习使用随机森林进一步改善模型。</p>
<h3 id="随机森林使用更复杂的机器学习算法">6.随机森林——使用更复杂的机器学习算法</h3>
<h4 id="简介-1">6.1 简介</h4>
<p>决策树留下了一个难以抉择的问题， 一棵叶子结点很多的的深树会导致过拟合，因为预测都是来自每个叶子结点上很少的历史数据。 然鹅，只有很少叶子节点的浅树性能也会很差，因为它无法捕获原始数据中许多的差异。</p>
<p>即使是当今最复杂的建模技术，也面临着欠拟合和过拟合之间的这种张力。 但是，许多模型都有巧妙的想法，可以带来更好的性能。 我们将以随机森林为例。</p>
<p>随机森林采用了结合很多决策树的方法，并且通过平均每个组成树的预测来进行预测。 与单个决策树相比，它通常具有更好的预测准确性，并且使用默认参数就可以获得较好的效果。 有许多性能更好的的模型，但其中许多模型对于获取正确的参数很敏感。</p>
<h4 id="举例-1">6.2 举例</h4>
<p>您已经看到了几次加载数据的代码。 在数据加载结束时，我们具有以下变量：</p>
<p>train_X val_X train_y val_y</p>
<p>我们构建随机森林模型（与scikit-learn中构建决策树的方式类似），使用<span class="math inline">\(RandomForestRegressor\)</span>类而不是<span class="math inline">\(DecisionTreeRegressor\)</span>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> RandomForestRegressor</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> mean_absolut_error</span><br><span class="line"></span><br><span class="line">forest_model=RandomForestRegressor(random_state=<span class="number">1</span>)</span><br><span class="line">forest_model.fit(train_X,train_y)</span><br><span class="line">melb_preds=forest_model.predict(val_X)</span><br><span class="line">print(mean_absolut_error(val_y,melb_preds))</span><br></pre></td></tr></table></figure>
<h4 id="结论-1">6.3 结论</h4>
<p>随机森林模型可能还有进一步改进的余地，但这是对最佳决策树error 250,000的重大改进。 有一些参数可以让您改变随机森林的性能，就像我们改变了单个决策树的最大深度一样。 但是，Random Forest模型的最佳功能之一是，即使不进行此调整，它们通常也可以正常工作。</p>
<p>您很快就会学到XGBoost模型，当使用正确的参数进行很好的调整时，它可以提供更好的性能（但是需要一些技巧才能获得正确的模型参数）。</p>
<h4 id="练习-3">6.4 练习</h4>
<p>数据科学并不总是那么容易。 但是用随机森林替换决策树将是一个轻松的胜利。</p>
<h5 id="使用随机森林模型">使用随机森林模型</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.ensemble <span class="keyword">import</span> RandomForestRegressor</span><br><span class="line"></span><br><span class="line"><span class="comment"># Define the model. Set random_state to 1</span></span><br><span class="line">rf_model = RandomForestRegressor(random_state=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># fit your model</span></span><br><span class="line">rf_model.fit(train_X,train_y)</span><br><span class="line">rf_val_predictions=rf_model.predict(val_X)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Calculate the mean absolute error of your Random Forest model on the validation data</span></span><br><span class="line">rf_val_mae = mean_absolute_error(val_y,rf_val_predictions)</span><br><span class="line"></span><br><span class="line">print(<span class="string">"Validation MAE for Random Forest Model: &#123;&#125;"</span>.format(rf_val_mae))</span><br><span class="line"></span><br><span class="line"><span class="comment"># Check your answer</span></span><br><span class="line">step_1.check()</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="机器学习比赛进入机器学习竞赛的世界">7 机器学习比赛——进入机器学习竞赛的世界</h3>
<h4 id="简介-2">7.1 简介</h4>
<p>机器学习竞赛是提高您的数据科学技能和衡量您的进步的好方法。</p>
<p>在本练习中，您将创建和提交Kaggle比赛的预测。 然后，您可以改进模型（例如，通过添加特征）来改进模型，并了解如何与其他人一起学习此微型课程。</p>
<p>本笔记本中的步骤为：</p>
<p>1.使用所有数据（<strong>X </strong>和<strong>y </strong>）构建随机森林模型 2.读入“test”数据，其中不包含目标值。 使用您的随机森林模型预测测试数据中的原始值。 3.将这些预测提交给比赛并查看您的分数。 4.（可选）返回以查看是否可以通过添加功能或更改模型来改进模型。 然后，您可以重新提交以查看如何将其堆积在比赛排行榜上。</p>
<h4 id="为比赛创建模型">7.2 为比赛创建模型</h4>
<p>建立随机森林模型，并在所有<strong>X </strong>和<strong>y </strong>上进行训练。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#为了提高准确性，请创建一个新的随机森林模型，您将在所有训练数据上进行训练</span></span><br><span class="line">rf_model_on_full_data = RandomForestRegressor（）</span><br><span class="line"></span><br><span class="line"><span class="comment">#适用所有训练集的数据来训练rf_model_on_full_data</span></span><br><span class="line">rf_model_on_full_data.fit（X，y）</span><br></pre></td></tr></table></figure>
<h4 id="作出预测">7.3 作出预测</h4>
<p>读取“test”数据文件。 并应用模型进行预测</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 用于预测的文件路径</span></span><br><span class="line">test_data_path = <span class="string">'../input/test.csv'</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用pandas读取测试数据文件</span></span><br><span class="line">test_data = pd.read_csv(test_data_path)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建来自test_data的test_X，但仅包含用于预测的列。</span></span><br><span class="line"><span class="comment"># 用于预测的列被存储在features的列表变量中</span></span><br><span class="line">test_X = test_data[features]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 做出我们将提交的预测。 </span></span><br><span class="line">test_preds = rf_model_on_full_data.predict(test_X)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 下面的代码行显示了如何将预测保存为比赛计分格式</span></span><br><span class="line"></span><br><span class="line">output = pd.DataFrame(&#123;<span class="string">'Id'</span>: test_data.Id,</span><br><span class="line">                       <span class="string">'SalePrice'</span>: test_preds&#125;)</span><br><span class="line">output.to_csv(<span class="string">'submission.csv'</span>, index=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>
<h4 id="测试您的工作">7.3 测试您的工作</h4>
<p>要测试您的结果，您需要参加比赛（如果尚未参加）。因此，通过单击[此链接]（https://www.kaggle.com/c/home-data-for-ml-course），打开一个新窗口。然后点击“参加比赛”按钮。</p>
<p>接下来，请按照以下说明进行操作： 1.单击此窗口右上角的蓝色<strong>COMMIT </strong>按钮开始。这将生成一个弹出窗口。 2.代码运行完毕后，单击弹出窗口右上方的蓝色“打开版本”按钮。这使您进入同一页面的查看模式。您将需要向下滚动以返回这些说明。 3.单击屏幕左侧的“输出”选项卡。然后，点击“提交竞赛”按钮，将您的结果提交到排行榜。</p>
<p>您现在已成功提交比赛！</p>
<p>4.如果要继续努力以提高性能，请选择屏幕右上方的蓝色“编辑”按钮。然后，您可以更改模型并重复该过程。有很多空间可以改善您的模型，并且您将在工作中攀登排行榜。</p>
<h4 id="keep-going">7.4 Keep Going！</h4>
<p>有很多方法可以改善您的模型，改善模型的最佳方法是添加特征。查看features列表，并考虑可能会影响房价的因素。由于缺少值或非数字数据类型等问题，某些功能会导致错误。</p>
<p><strong>[中级机器学习]（https://www.kaggle.com/learn/intermediate-machine-learning）</strong>微型课程将教您如何处理这些类型的功能。您还将学习使用** xgboost **，该技术比随机森林具有更高的准确性。</p>
<p>＃其他微课程 <strong>[Pandas]（https://kaggle.com/Learn/Pandas）</strong>微课程将为您提供数据处理技能，使您可以从概念概念快速实施到数据科学项目中。</p>
<p>您还可以参加<strong>[Deep Learning]（https://kaggle.com/Learn/Deep-Learning）</strong>微课程，在那里您将在计算机视觉任务上构建性能优于人类水平的模型。</p>
]]></content>
      <categories>
        <category>Machine Learning</category>
      </categories>
      <tags>
        <tag>ML</tag>
        <tag>Kaggle</tag>
        <tag>tutorial</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/ck7mctjgs000hf4vs84u0h79n/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p>
<h2 id="quick-start">Quick Start</h2>
<h3 id="create-a-new-post">Create a new post</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p>
<h3 id="run-server">Run server</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p>
<h3 id="generate-static-files">Generate static files</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p>
<h3 id="deploy-to-remote-sites">Deploy to remote sites</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>About Me</title>
    <url>/2020/about/</url>
    <content><![CDATA[<h1 id="section"></h1>
<p>姓名：黑头呆鱼</p>
<p>微信：yvette-chen-</p>
<p>GitHub：<a href="https://github.com/chenhuiyu" target="_blank" rel="noopener">没有任何干货的GitHub</a></p>
<p>研究方向：:shit:</p>
<a id="more"></a>
<figure>
<img src="https://i.loli.net/2020/03/08/t4R9hnpPqMXfS5m.jpg" alt="烦死了.jpg" /><figcaption>烦死了.jpg</figcaption>
</figure>
]]></content>
  </entry>
</search>
